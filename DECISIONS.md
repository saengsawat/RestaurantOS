# DECISIONS.md

Running log of project decisions. Nothing is decided until it is here. Mirrors master plan §12; this file is the working copy, the plan is updated at phase gates.

| # | Date | Decision | Status |
|---|---|---|---|
| D1 | 2026-08-11 | RestaurantOS is a POS; intelligence layer is the differentiator, not a separate product | Decided |
| D2 | 2026-08-11 | Integrate payments (Stripe Terminal / Adyen); never build processing; never store PAN | Decided |
| D3 | 2026-08-11 | Modular monolith + PostgreSQL + transactional outbox; no microservices/K8s for V1 | Decided |
| D4 | 2026-08-11 | Local-first write ordering; frontend is never the ledger | Decided |
| D5 | 2026-08-11 | Build with Claude Code per the agent operating model (plan §5) | Decided |
| D6 | | LAN-continuous service during WAN failure = P0? | **Open: Phase 0 gate, needs Matt** |
| D7 | | Stack (React/TS/PWA leading candidate) | Open: Phase 2 ADR-1 |
| D8 | | Payment provider (Stripe vs Adyen) | Open: Phase 2 ADR-3 |
| D9 | | Client platform (PWA/Electron/native) | Open: Phase 2 ADR-4 |
| D10 | | Pilot venue | Open: Phase 0/5 |
| D11 | 2026-08-11 | Design language: RestaurantOS system derived from Wise (`design/restaurantos/`) | Decided |
| D12 | 2026-08-11 | Writing style: no em dashes in any content (docs, UI, commits, chat) | Decided |
| D13 | 2026-08-12 | Phase overlap: Matt-independent Phase 1/2 work starts now (domain model, schema, PRD draft). Matt's answers retrofit via change control; anything D6-dependent (edge/LAN, stack ADR) stays frozen | Decided (founder) |
| D14 | 2026-08-12 | Schema conventions: client-generated UUID ids, BIGINT minor units for all money, TEXT + CHECK for enums, immutable JSONB menu snapshots, org/location ids on every operational table, computed (never stored) totals | Decided, see `docs/domain/schema.sql` header |
| D15 | 2026-08-12 | Domain language: TypeScript (strict), app tree at `app/`, first package `app/domain` (pure, no I/O). Client framework and server framework remain open (ADR-1 full ratification still waits on D6) | Decided (founder go-ahead) |
| D16 | 2026-08-12 | Server skeleton ships now on Fastify (provisional; matches the leading-candidate stack, swap cost is one thin route layer). Rationale: D6 gates edge/LAN topology, not the cloud command API, and the founder expects a runnable server. Store is in-memory behind a Store interface; the PostgreSQL repository (E4) implements the same interface against schema.sql | Decided (founder) |
| D17 | 2026-08-20 | Worker model pool widened: external models (e.g. Codex) may take Sonnet-class tickets (§5.2 row 3: UI screens, fixtures, scaffolding) under the same ticket contract, review, and escalation rules. Money-touching and cross-module work stays Opus/Fable-class. Founder asked for this explicitly | Decided (founder) |
| D18 | 2026-08-20 | E11 v1 boundary: a split is a PAYMENT PARTITION over one check (preview computed on read, portions settled by labeled payments), not a fork into sibling check aggregates. Party-level check splitting at open time is deferred to a Matt conversation (deck A) | Decided (orchestrator, ticket E11-T2) |
| D19 | 2026-08-20 | Insights v1 (server performance report + sales heatmap) pulls forward NOW as read-only projections over the ledger (new epic E19), inspired by competitor observation (Lightspeed Restaurant). Nothing is stored; reports compute on read, same rule as totals. The guestbook is spec-only for now (E20) | Decided (founder) |
| D20 | 2026-08-20 | Guest identity ladder for E20: manual attach (name/phone) first, phone lookup second, automatic card-fingerprint recognition only after E13 lands (provider tokens only, never PAN, per D2). Privacy questions (consent, retention, staff visibility, deletion) go to the Matt deck with recommended defaults | Decided (founder direction, spec in E20-T1) |
| D21 | 2026-08-20 | Role boundary: the orchestrator session (Fable) plans, writes ticket contracts, assigns models, and reviews; it does NOT write product code. All build work is delegated to worker sessions (Opus for money/domain/cross-module, Sonnet/Codex for UI/docs/scaffolding per D17). Fable serves as the cross-model reviewer for Opus work | Decided (founder) |
| D22 | 2026-08-22 | Ticket batching: at orchestrator discretion, ONE worker session may execute two or more small same-model tickets STRICTLY SEQUENTIALLY, with one clearly labeled commit (group) per ticket and the full suite green at each boundary. Commits are the review unit, so verdicts stay separable (a passing ticket merges even if its batchmate fails). Parallel sessions remain forbidden (shared working folder) | Decided (founder + orchestrator) |
| D23 | 2026-08-23 | Guestbook v0 builds NOW (supersedes D19's spec-only stance): manual-attach rung only, per `docs/prd/guestbook-spec.md`, privacy defaults shipped as coded defaults pending Matt's deck C answers. Motivation: a working "we remember your regulars" demo for the Matt conversation. Fingerprints stay E13-gated, allergen tags stay C5-gated | Decided (founder) |
| D24 | 2026-08-23 | Naming: the live reporting screen is **Reports** (`/reports`); **Insights** is reserved for the Phase 6 intelligence layer (the mockup's "data turned into decisions") and leaves the live app until that layer exists. API paths (`/v1/insights/*`) keep their names: not user-facing copy, not worth the churn | Decided (founder, name proposed by orchestrator) |
| D25 | 2026-08-23 | POS UI parity with the flagship mockup: the mockup is the binding UX reference for the live POS (the master plan already named it the [S] reference; the founder now directs applying it). First wave: left-rail app shell per DESIGN.md, menu category rail, per-course hold/fire, check-panel action bar (Discount/Void multi-select/History/More), line tap never voids. Tickets E8-T3, UI-T1/T2/T3 | Decided (founder) |
| D26 | 2026-08-25 | RestaurantOS must run any restaurant, not just the demo venue. BUILD now: the full floor editor (add/edit/resize/retire tables and areas, manager-gated, retire is soft so history survives; decor elements later) and venue settings + staff management (name/address/timezone as data, roster CRUD with hashed PINs, last-manager guard; demo venue stays the seed). SPEC-ONLY: POS migration/import (menu via CSV into draft-then-publish, floors redrawn, fresh PINs always; designed against a real customer export before any build). Tickets E6-T2/T3, E21-T1/T2, E22-T1 | Decided (founder) |
| D27 | 2026-08-28 | Reservations: a reservation is a PROMISE, not a lock (the floor warns, the host overrides, never a hard refusal). v1 is the staff-entered call-in book, which also feeds the D20 phone-lookup rung; online booking is "integrate, never build" pending Matt (deck E). Spec first: E23-T1 | Decided (founder) |
| D28 | 2026-08-28 | Team & labor ladder: rung 1, the people directory, builds now (job TITLE separate from permission LEVEL; home contact details are manager-PIN-gated PII); rung 2, scheduling (planned vs actual shifts), waits on Matt (deck F); rung 3, payroll, is NEVER computed here: hours + declared tips export as CSV for a payroll provider, the payments posture applied to labor. Tickets E24-T1 (spec), E24-T2 (directory) | Decided (founder) |
| D29 | 2026-09-01 | E5-full's finish line is USER value, not storage shape: the epic completes when a manager can edit the whole menu graph (items, modifier groups, min/max, options, prices, assignments) through the existing draft-then-publish flow. The draft STAYS a document; relational draft storage is deferred until a real menu import demands it. Rung 3's hours export (D28) is greenlit to build in the same wave. Tickets E5-T2/T3, E24-T3 | Decided (founder) |
| D30 | 2026-09-02 | Migration builds its thin end NOW: the menu CSV import (our template, landing as an idempotent draft with provenance, never publishing itself), because E5-full stabilized the menu data model and the menu is the onboarding long-pole; a working import is also the second-restaurant pitch (the Nine Thai scenario). Still parked: vendor-format converters (until a real EXPORT file exists; the NorthStar artifact is a load template), guest imports (Matt consent deck), staff CSV (a hire is five seconds). Tickets E22-T2/T3 | Decided (founder) |
| D31 | 2026-09-02 | Reservations (the D27 call-in book, per reservations-spec) and scheduling (rung 2 of D28, per team-labor-spec) BUILD NOW, in-house and phone-first, without waiting for Matt's decks E/F; his answers retrofit via change control like D13. Online booking stays integrate-never-build; deposits stay E13-gated; the schedule keeps the draft-then-publish gate. Tickets E23-T2/T3, E24-T4/T5 | Decided (founder) |
| D32 | 2026-09-02 | Settings input posture: the timezone picker stays first-party (the browser ships the full IANA zone list via Intl.supportedValuesOf; already built in E24-T3's settings work, discoverability improved in E21-T3). Address AUTOCOMPLETE is inherently third-party (Google Places / Mapbox / OSM Nominatim: a global address database cannot ship in a zero-dependency page) and is PARKED until an online-integrations ADR; the address stays free text, which the receipt renders verbatim either way | Decided (founder question, orchestrator recommendation) |
| D33 | 2026-09-02 | Roles and visibility: the permission enum grows from server\|manager to **owner \| manager \| kitchen \| server** with a per-screen visibility matrix enforced SERVER-SIDE (nav hiding is courtesy, the route check is the rule). Owner outranks manager (venue identity, pay period, and role changes on managers are owner-gated); kitchen and server are lateral, not ranked. D28's rule stands: TITLE stays vocabulary (Chef, Bartender are titles on a kitchen-level person), permission is the enum. Epic E25 | Decided (founder) |
| D34 | 2026-09-02 | Inventory goes SPEC-FIRST (E26-T1): ingredient-level stock with per-item recipe quantities (a Branzino al Forno depletes one fish), depletion computed from kitchen fires, par levels, and a chef-facing order list; the bar is the same mechanism with bar-station items. The founder (7 years restaurant, sous chef) is the domain reviewer, not Matt. Builds only after that review; ties into (never replaces) the existing 86/count board | Decided (founder) |
| D35 | 2026-09-03 | The app grows TWO FACES instead of one ever-longer rail. The FLOOR keeps the rail, zero taps to work, role-filtered per D33 (absent, never grayed): Service, Tables, Bookings, Kitchen, Close, Shifts (personal, so it stays). The BACK OFFICE (Menu, Reports, Settings, and every management surface still to come: inventory, guests, food cost) collapses into ONE rail entry, "Office", opening a card hub in the founder's portal style but on our design system: one card per area with a one-line live stat from existing reads, cards role-aware from the same session matrix, sub-links where a page has sections. Sign-in lands by role: server on Service, kitchen on Kitchen, manager and owner on the hub. Rationale: a launcher is right for surfaces visited occasionally and wrong in front of a screen tapped 200 times a shift; incumbents split POS from back office for the same reason. Ticket UI-T5, after E25-T2 | Decided (founder direction, orchestrator shape) |
