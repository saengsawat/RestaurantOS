# BACKLOG.md

The authoritative ticket list (master plan §5.5). Currently tracking Phase 0 work packages; build epics (E1..E18) arrive in Phase 3.

## Phase 0: Close out discovery

| WP      | Item                                                                                                                       | Status                                 | Owner        |
| ------- | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------ |
| WP-0.1  | Repo under git, layered layout, demo doc (README)                                                                          | **Done** 2026-08-11              | Orchestrator |
| WP-0.2  | Operator sessions with Matt (guide:`docs/discovery/operator-session-guide.md`)                                           | **Ready, waiting on scheduling** | Andy + Matt  |
| WP-0.2a | Send Matt the walkthrough (`docs/RestaurantOS_User_Guide.docx` / `.pdf`) plus the live demo link, ahead of the session | **Ready to send**                | Andy         |
| WP-0.3  | Discovery notes, labeled OBSERVED/INFERRED                                                                                 | Blocked by WP-0.2                      | Andy         |
| WP-0.4  | Pilot-selection criteria (draft in session guide, finalize with Matt)                                                      | Draft done                             | Andy + Matt  |
| WP-0.5  | Freeze D6 (LAN survival P0?) in DECISIONS.md                                                                               | Blocked by WP-0.2                      | Andy         |

**Phase 0 exit:** question deck answered (or explicitly deferred), D6 frozen, pilot criteria final.

## Phases 1-2, Matt-independent track (opened early per D13)

| WP      | Item                                                                                                                                      | Status                                   | Owner                         |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------- |
| WP-2.1a | Database schema (`docs/domain/schema.sql`, 44 tables) verified against PostgreSQL 17: clean apply, FK chain, constraint tests           | **Done** 2026-08-12                | Orchestrator (Opus-tier work) |
| WP-2.1b | Domain model doc (`docs/domain/domain-model.md`): aggregates, two state machines, command surface, invariants ledger, Matt-impact table | **Done** 2026-08-12                | Orchestrator                  |
| WP-1.1  | Draft`docs/prd/RestaurantOS_POS_PRD.md`: 38 FRs traced to epics, defaults on every UNKNOWN so nothing blocks                      | **Draft done** 2026-08-12; Matt review pending (WP-1.2)                                 | Orchestrator                  |
| WP-2.2  | ADR-1 (stack), ADR-2 (edge/LAN)                                                                                                           | **Frozen on D6 (Matt)**            | Founder + Orchestrator        |
| WP-2.2b | ADR-3 (payment provider), ADR-4 (client platform)                                                                                         | Draftable as comparisons; decision waits | Orchestrator                  |
| WP-1.2  | Matt review cycle, scope signoff (V1 / P1 / P2 / never)                                                                                   | Blocked on Matt                          | Andy + Matt                   |

## Phase 4: first code (opened per D13 + D15)

| Epic | Ticket | Status |
|---|---|---|
| E1 money engine | `app/domain/src/money.ts`: applyRate (single half-up rounding site), allocateEvenly, allocateByWeights (largest remainder, BigInt-exact), lineTotalMinor, computeCheckTotals. 21 tests green incl. properties: conservation, one-cent fairness, determinism, weights-identity, end-to-end split-after-tax conservation | **Done** 2026-08-12 |
| E1 follow-ups | Tip allocation across splits, cash rounding for non-US currencies, tax-inclusive pricing mode | Backlog, pilot-dependent |
| E2 state machines | `checkLifecycle.ts` + `kitchenLifecycle.ts`: pure transition functions, guards as booleans from the command layer. Exhaustive tables (all 66 check pairs asserted) + random-walk properties (voided inescapable, closed exits only via approved reopen) | **Done** 2026-08-12 |
| E3 modifier validation | `modifiers.ts`: min/max/duplicates/nesting/depth validator, error-collecting, total over corrupt snapshots; selectionPriceMinor + defaultSelections. Fixture suite + generated-menu properties | **Done** 2026-08-12 |
| E4 persistence | `pgStore.ts` + `migrations/`: PostgreSQL Store against the 44-table schema. Checks, lines (selection tree in jsonb per migration 0002), payments (intent/attempt/payment rows), tickets (dispatch + kitchen_ticket + per-item flags), sync journal, floor seed. Integration test spins a throwaway PG 17, runs a full service, then rebuilds the store to prove restart survival | **Done** 2026-08-12 |
| E7 check engine | Command layer with idempotent operation ids + optimistic versions (shipped with the server) | **Core done** |
| E7 transfer + merge | Transfer moves a check to a free table (kitchen cards and the party row follow; occupied targets refuse). Merge combines two checks with manager approval: covers add, source seats renumber after the target's covers so "seat 2" stays a real person, fired lines stay fired, source must be unpaid (refund first) and voids with the merge as its paperwork. Floor derives kitchen-late by TABLE so absorbed courses still count. POS: the ⋯ button on the check header | **Done** 2026-08-19 |
| E8 dispatch + KDS | Send creates one dispatch ticket per course; `/kds` page: one card per table, New flags, per-item bump, expo-only gated serve, 10-min recall, cook-together pane | **Done** 2026-08-12 |
| E6 floor | `/v1/floor` + `/tables` page: spatial room per section, live status (open/seated/paying/late) derived from checks + tickets, tap-to-seat opens a real check, occupied tables refuse a second check | **Done** 2026-08-12 |
| E6 layout editor | "Edit layout" mode on `/tables`: drag a table, drop saves to the server (`/v1/floor/move`, clamped to the room, positions in `dining_table.pos`), every device sees the new room. Follow-ups: resize, add/remove tables, overlap warning | **Done** 2026-08-18 |
| E12 voids + discounts | Server commands with the paperwork the schema demands: void needs reason + manager approval (demo 4-digit PIN until E15), fired voids flag the kitchen line so the cook stops and serve does not wait on them; discounts/comps are amount XOR percent into `check_adjustment`, capped at subtotal, refused after payment. POS: tap a line to void, % button for discounts; KDS renders voided lines struck + red | **Done** 2026-08-18 |
| E5 menu domain | Full loop live at `/menu`: draft editing (add/edit/remove items), manager-gated publish freezing the draft into the next immutable `menu_snapshot` version, service repricing on the new snapshot while ordered lines never move (each line records its snapshot id). Live 86 board on `item_availability`: instant 86, running counts that auto-86 at zero, enforced at add-item, badged on POS tiles. v0 note: draft is a document (migration 0003) until the relational editor arrives; group editing is E5-full | **Core done** 2026-08-19 (epic 🔄 until E5-full) |
| E14 cash management | Drawer sessions on `/close`: open with counted float (one open session per drawer, like `idx_drawer_one_open`), cash payments refuse without an open till and land as `sale` events, pay-in free / pay-out + drop need a manager, count-and-close freezes expected + over/short forever. Cash events append-only into `cash_event` | **Done** 2026-08-18 |
| E16 business-day close | `/close` page: live day summary (net, tax, tips, card/cash, voids), blockers list (open checks with jump links, open drawers, offline payments), manager-gated close that seals the day, reopen for corrections. Business day = server-local date of opening (`serviceDateOf`); rollover hour is pilot config. New checks refuse while the day is closed | **Done** 2026-08-18 |
| E15 staff identity | Real PIN sessions and roles: hashed PINs on `employee`, roles seeded (`role`, `employee_role`), manager approvals now verify the PIN belongs to an actual manager (a server's PIN refuses with "not recognized as a manager"). Sign in per device (`/v1/session`); payments, voids, discounts, drawers, and the sync journal record WHO (taken_by, void_approved_by, applied_by/approved_by, opened_by/closed_by, sync_operation.employee_id). Demo roster: Gia R. server 2468, Marco B. manager 1122, Sofia T. server 3579. Sign-in UI on POS header | **Done** 2026-08-19 |
| Lock screen | `/` is now the app entry: a PIN-pad lock screen (POS pattern: the device is trusted, the person identifies by PIN). Sign-in creates the device session and lands on Service; sign-out and clock-out return to it. API reference moved to `/api` | **Done** 2026-08-20 |
| E9 slice: receipts | Guest receipt from the ⋯ menu on any check: itemized with modifiers, voids struck (visible by design), discounts, tax, payments with tips and pending-upload honesty, print via `@media print`. Follow-ups: per-seat split receipts, real printer integration | **Done** 2026-08-20 |
| Reopen check | `POST /v1/checks/:id/reopen` (manager PIN) through the state machine's reopen transition; Close page lists today's closed checks with a Reopen button; the check returns to Service | **Done** 2026-08-20 |
| E14 slice: shifts + tips | Sign-in auto-clocks-in (shift rows persisted); clock-out declares cash tips confirmed by the employee's own PIN (POS who-menu or Close page Team section); open shifts BLOCK the day close; declared tips reported on /close | **Done** 2026-08-20 |
| E5 fix (founder report) | Live menu items could not be edited or removed (a Beer added without a price was stuck). Every live row now has Edit and Remove, routed through the draft + manager publish (never mutating the live snapshot); rows already removed on the pending draft say so | **Done** 2026-08-20 |
| **E11-T1** split domain | Pure `splitCheck` in `app/domain`: `even` (N ways) and `byLines` (seat or item) partitions. Totals computed once, then every component allocated with the E1 largest-remainder allocators, never recomputed per portion. Conservation, row consistency (total = subtotal - discount + tax), non-negativity and determinism are property-tested; refuses ways < 2, wrong-length or non-dense assignments, byLines on an empty check. Contract: `docs/tickets/E11-T1-split-domain.md` | **Merged** 2026-08-20 · cross-model review (Fable) PASSED: independent test run, allocator-bound proofs re-verified, scope clean |
| **E11-T2** split engine/API | `GET /v1/checks/:id/split?mode=even&ways=N` or `?mode=bySeat`: portions with label, seats, every money component, paid and due, computed on read (nothing stored). bySeat gives a portion only to seats holding non-voided lines, per T1's dense-assignment note. Labeled payments settle a portion and are refused past its remaining due plus tip; the check still closes only on TOTAL payments covering the total. Engine does no portion arithmetic: `splitCheck` does the money. Contract: `docs/tickets/E11-T2-split-engine-api.md` | **Merged** 2026-08-20 · cross-model review (Fable) PASSED: suites re-run on the branch (104), overpay guard and empty-seat handling verified against the contract |
| **E11-T3** split POS UI | Pay modal split selector (Whole check / Even with a 2, 3, 4, custom ways picker / By seat), portion cards showing each portion's due and paid state from the server preview, Pay per portion with the tip percent applied to that portion. Receipt gains a portion picker (whole check or one slip per portion; seat slips carry that seat's lines, even slips say they are a share), header chip reads `split: even 3` or `split: by seat`. Offline portion payments stay amber pending upload. Contract: `docs/tickets/E11-T3-split-pos-ui.md` | **Merged** 2026-08-22 · review (Fable) PASSED: suite re-run, added-CSS token audit clean, desktop + 390px screenshots taken (the visual pass the worker could not do); the cross-partition finding became E11-T4 |
| **E8-T2** KDS/check drift guardrail | The rail read joins each ticket to its check's paying status (`checkStatus`, read-time only, nothing stored). A KDS card whose every open dispatch belongs to a paid or closed check carries a `check closed` / `check paid` amber chip and drops out of the red-late escalation: settled guests are a cleanup task, not a late table. Open kitchen tickets are now a day-close blocker named by table and course, rendered on `/close` with a link to the Kitchen screen. No auto-bump, no auto-serve, no state machine change. Contract: `docs/tickets/E8-T2-kds-check-drift.md` | **Merged** 2026-08-22 · review (Fable) PASSED: suite re-run at both D22 commit boundaries, founder scenario reproduced live end to end (chip shown, late gone, close blocked then sealed after sweep), /kds and /close screenshots verified |
| **E11-T4** cross-partition guard | A labeled portion payment is now capped by `min(portion due, check due)` in `recordPayment`, so money already settled under another partition's labels cannot be paid twice. The refusal names both figures when the check's due is the binding constraint. Unlabeled payments unchanged (cash expecting change stays legal); no new state, so D18 holds. Contract: `docs/tickets/E11-T4-cross-partition-guard.md` | **Merged** 2026-08-22 · review (Fable) PASSED: guard re-verified live over the exact T3 defect (422 naming both amounts), property loop and revert-check read. Epic E11 closes with this. Noted follow-up: a caller lumping the tip into amountMinor can still push non-tip money past the check total; tips must travel in tipMinor (our POS does). Tighten if a second client ever appears |
| **E19-T1** insights core | Every check now records WHO opened it (the employee signed in on the device, falling back to the seeded default so an unsigned terminal still attributes somewhere real), persisted as `checks.server_id` and read back off the employee join; the close also stamps `party.cleared_at`, the turn-time endpoint. Two read-only projections over that ledger: `GET /v1/insights/servers` (today's closed checks grouped by server, sorted by net sales, exact integer sums for checks/covers/net/total/tips/declared tips/discounts/voids and a per-course value map, with avg check, per cover and turn minutes derived from those sums, plus an Average row and the non-zero `courseKeys`) and `GET /v1/insights/heatmap` (day-of-week x hour cells of net/checks/covers over every closed check the store holds, `dayTotals`, `grandNetMinor`, `daysCovered`). Nothing stored, no schema change, `toView` stays the single money source. Conservation is tested: per-server net sums to the day's gross minus discount, per-server tips to the day's tips, heatmap grand net to the same figure; voided lines count as voids only, never as course value or net. Demo note: with two servers signed in and four checks closed, `curl http://localhost:3000/v1/insights/servers` returned Sofia 2400 net / 200 tips above Gia 1600 / 300 with an Average row at 2000, and `curl http://localhost:3000/v1/insights/heatmap` returned one Saturday-22:00 cell of 4000 net / 2 checks / 4 covers, matching `/v1/day`'s gross 4000 and tips 500 exactly. Contract: `docs/tickets/E19-T1-insights-core.md` | **Merged** 2026-08-23 · review (Fable) PASSED: suite re-run on the branch (46 server incl. PG join + cleared_at assertions), conservation re-verified live with two signed-in servers (per-server net and tips matched /v1/day to the cent, heatmap grand net identical, rows sorted by net). Known nuance, deliberate: the scorecard rows come from closed checks, so an employee who declared tips but closed no check (e.g. a manager) has no row; E19-T2 must not present the summed row tips as the day's declared total, the day report owns that number |
| **E19-T2** insights page | `/insights` (new `public/insights.html`, served beside `/close`, linked from the nav of all six pages): Tonight at a glance tiles (covers, checks closed, net sales, avg check, tips as card + declared), a server scorecard of horizontal stacked bars (bar length = that server's net against the top server, segments = their course mix in `courseKeys` order, one status wash per course on every row, muted Average row), and a Busiest times grid of hour x day cells shaded by net against the busiest cell with a per-day share footer. Tapping a server opens a Lightspeed-style card: rank, actual, average and a green/red variance percent on eleven metrics, plus that server's net by course against the average (so the segment values a bar shows on hover are reachable by tap too). Tapping a cell gives its net, checks, covers and share. Every figure comes from the two E19-T1 endpoints verbatim; the only arithmetic on the page is percent-for-pixel-width and the variance percent, and empty states say so rather than drawing fake bars. Polls every 15s, paused while a card is open. Demo note (live, two servers, five closed checks): scorecard read Gia R. $392.00 (bar 100%) over Sofia T. $227.00 (57.91%) with the Average at $309.50 (78.95%), tiles read 15 covers / 5 checks / $619.00 net / $126.00 tips (card $92.00 + declared $34.00) matching `/close` exactly; Gia's card showed net sales +27% and void value +100% against the average; the single populated heatmap cell opened at $619.00, 5 checks, 15 covers, 100% share. Verified in Chrome at 1280px Day and Night and at 390px (topbar stays one row, bars stack, the grid scrolls in its own box, the page body never does), all page targets at least 44px, script parses under `node --check`. Contract: `docs/tickets/E19-T2-insights-page.md` | **Merged** 2026-08-23 · review (Fable) PASSED with one confirmed defect that traces to the ticket, not the worker: the Tips tile's declared figure sums the scorecard rows, and rows exist only for employees who closed a check, so a declaration from someone without a row (Marco at day close) is invisible; reproduced live (day declared 3400, tile source 0). Fix is E19-T3. Everything else verified independently: suite re-run (47), typecheck, `node --check` on the page script, no em dashes, Day/Night at 1280px and 390px screenshots (token palette holds in Night, topbar one row on mobile, grid scrolls in its own box), press feedback and 44px targets in the source |
| **E19-T3** declared-tips total | `insightsServers()` returns a top-level `declaredTipsTotalMinor`: the exact integer sum of `declaredTipsMinor` over the shift window the method already uses (`clockIn` today or still open), across ALL employees, scorecard row or no row. The /insights Tips tile reads that instead of summing rows, so its declared figure equals `/close` unconditionally. Rows are untouched: a row still carries only that server's own declaration, and the Average row is unchanged. Still a pure read, integer addition, nothing stored. Demo note (the E19-T2 review's scenario, live): Gia and Sofia close a check each, Marco the manager clocks out declaring 3400 without ever closing one. Before, the row sum was 0 and the tile printed "declared $0.00" while `/close` showed $34.00. After, the rows still read Gia 0 / Sofia 0 (their own declarations), `declaredTipsTotalMinor` is 3400, `/v1/day` reports 3400, and the rendered tile reads "Tips $44.00 · card $10.00 + declared $34.00 (every shift)" beside `/close`'s "Card tips $10.00" and "Declared cash tips $34.00". Test proven both ways: it fails with "expected undefined to be 4600" against the pre-fix engine. Contract: `docs/tickets/E19-T3-declared-tips-total.md` | **Merged** 2026-08-23 · review (Fable) PASSED: suite re-run (48), typecheck, no em dashes, and the original bug scenario re-run live (Marco declares 3400 with zero checks: total 3400, /v1/day 3400, rows honestly 0, tile screenshot shows declared $34.00). Epic E19 closes with this |
| **E20-T1** guestbook spec | `docs/prd/guestbook-spec.md` (123 lines, POS PRD voice with evidence labels and a recommended default on every UNKNOWN): why regulars are the business and how the guestbook is the Observe rung of the Phase 6 ladder; a two-table entity proposal in the schema's own D14 conventions (`guest` identity only, `check_guest` attaching zero or more guests to a check) with everything else derived by join, plus the E13-only `guest_card_fingerprint` sketch so it is agreed in advance that only an opaque provider token is ever stored (D2, never PAN); the D20 identity ladder (v0 manual attach, v1 phone lookup, v2 card-fingerprint proposal a human confirms) with the two rules it never breaks and the guest-merge problem named, manager-gated and money-safe because history is a join and not a copy; profile contents all derived on read like the E19 projections (favorites, total and average spend, visits and cadence, last visit, preferred section and server, tip percent of net, allergen tags, notes, optional percentile polish), with multi-guest spend attribution defaulted to the E11 `splitCheck` allocators so per-guest spend still sums to the check to the cent; seven privacy questions for Matt, each with the default we would ship, and local law flagged as its own UNKNOWN; dependencies and sequencing that keep this out of V1 pilot scope unless Matt disagrees. Cross-references in place: one line in the Operator Console PRD's future-direction list, and deck C in `docs/discovery/operator-session-guide.md` carrying the seven questions verbatim with their defaults. No code, no migration, no UI. Contract: `docs/tickets/E20-T1-guestbook-spec.md` | **Merged** 2026-08-23 · review (Fable) PASSED: spec read in full against D2/D14/D19/D20 (no card data anywhere, opaque provider token only and E13-gated; profile derived on read; human-confirmed identity ladder; shared-check spend through the E11 allocators), deck C and the Operator Console PRD cross-references verified, no em dashes, docs only as scoped |
| **E20-T2** guestbook core | The spec's v0 rung as engine + stores. Two tables (migration `0004_guestbook.sql`, verbatim from spec §2, plus memory-store equivalents behind eight new `Store` methods): `guest` is identity only, `check_guest` attaches zero or more guests to a check, and there is no third table because every figure on a profile is a join. Commands through the envelope: create (name required, trimmed, `marketing_opt_in` false per privacy default C6), update (an empty string clears a field), attach and detach (allowed on a CLOSED check, since that is when a server has time to type who sat there; attaching twice is a no-op), manager-gated merge (links repoint skipping duplicates, notes append rather than drop, contact details fill gaps, absorbed record deleted, audit carries actor + approver + both names) and manager-gated delete (identity and links go, every check stays). Reads: `GET /v1/guests?q=` (case-blind substring over name and phone, capped at 25 with a total) and `GET /v1/guests/:id`, the derived profile computed on read like the E19 projections: favorites by count with a recency tiebreak, visit count, distinct service dates, median gap days, last visit, preferred section and server (mode with a recency tiebreak), tip percent of net. Spend is the one place money is touched: one guest owns the check, several split it through the domain's even allocator (v0 has no guest-to-seat mapping) and those visits carry `sharedCheck`. Attach state rides every check view as `guests: [{id, name}]`, joined at read time in `run()` and both check reads, so E20-T3 needs no second fetch. Conservation tested at two and three guests, the three-guest case with a discount in play: 871 + 871 + 871 = 2613. Merge and delete proven not to move a cent (every involved check's totals byte-identical before and after) and refused for a server's PIN. Demo note (live, curl): create returned Elena Rossi with `marketingOptIn:false`, attaching Elena then Paolo to a CLOSED check returned both chips, and their profiles read 2178 + 2177 = 4355 against the check's own 4355 total, with tip 17.5% of net, section Sala, server Gia R., favorites Acqua Panna x2, Tiramisu x1, Burrata x1. PG round trip: create + attach, restart the store, profile byte-identical. Contract: `docs/tickets/E20-T2-guestbook-core.md` | **Merged** 2026-08-23 · review (Fable) PASSED: suite re-run (55 server incl. PG round trip), typecheck, no em dashes, and the whole flow re-run live (three guests on a 3049 check share 1017+1016+1016 exactly; merge refused for a server PIN, accepted for a manager, check totals byte-identical; delete unlinks and search finds nothing). Judgment call accepted: deleteGuest removes the row outright, the strongest reading of C7 given no tombstone column, matching the merge path |
| **E19-T4** rename to Reports | `public/insights.html` is now `public/reports.html`, served at `/reports`, with `/insights` answering 302 to it so bookmarks and muscle memory still land. Tab title, topbar heading, and the nav pill on all six pages read Reports; "Insights" no longer appears in live page copy, which is what D24 reserved it for. API paths keep their `/v1/insights/*` names on purpose, with a comment in `server.ts` saying why (a path is not user-facing copy). Zero behavioral change otherwise: same markup, same data, same 15s polling. Verified live: `/reports` serves 200, `/insights` returns 302 with `location: /reports`, and following it renders the heading "Reports · Osteria Nove" with the Reports pill active. Contract: `docs/tickets/E19-T4-rename-reports.md` | **Merged** 2026-08-23 · review (Fable) PASSED: suite green at both D22 boundaries, /reports 200 and /insights 302 verified live, POS nav shows Reports in screenshot, API paths untouched as scoped |
| **E20-T3** guestbook screens | The check's More menu opens **Guests · attach a regular**: a search box live against `GET /v1/guests?q=` (name or phone, 200ms debounce, the box is built once so a fresh result set never steals the caret), result rows that tap through to a profile with an Attach button on the right, and a quick-create footer that turns an unmatched name into a guest attached to this check in one tap. Attached guests ride the check header as neutral 44px chips (the `guests` array the check view carries since E20-T2); a chip only OPENS the profile, and Detach lives in the profile's footer, so a stray tap on the header cannot silently unlink anybody. The profile pane prints `GET /v1/guests/:id` in spec §4 order: spend and average and visits and tip as stat tiles, favorites as count chips, habits (cadence, service days, usual section, usual server), the visit list with a `share of N` chip on shared checks, editable notes saved through `updateGuest`, and manager-gated Merge (search the duplicate, pick it, PIN, a confirm button naming both records) and Delete (PIN, red outline, the C7 wording: the checks stay, the person is unlinked). Empty states are honest at every level: no guests yet, nobody by that name, and "No visits yet. Attach this guest to a check and the profile builds itself." Tokens only, no new colors (guest pills are neutral because the status triples mean order state), 44px targets measured at 390px, Day and Night, modal scrolls inside itself. Nothing is stubbed: every control reaches a real E20-T2 command. Demo note (click path driven in Chrome, screenshots taken): More > Guests on an empty book, type "Elena Rossi", the footer offers Create and attach, one tap creates and attaches and the chip appears on the header; a second guest attaches the same way; tapping a chip opens the profile; notes save; Merge picks Paolo, names both records, and the toast reads "Paolo Rossi merged into Elena Rossi" with the chips collapsing to one; Delete with a server PIN is refused ("PIN not recognized as a manager") and with the manager PIN clears the record while the check's totals stay $40.00 / $3.55 / $43.55 exactly. With a real history seeded (a closed check of $60.97 attributed to Elena), the populated pane reads total spend $60.97, average $60.97, 1 visit, tip 16.1%, favorites 1× Chianti Classico and 1× Burrata, usual section Sala, usual server Gia R., and the visit row Aug 23 · Table 14 · #2042 · $60.97. One honest deviation: result rows show name, phone, and notes rather than last visit, because the search endpoint returns guest records without visit history and fetching 25 profiles per keystroke would be absurd; last visit shows in the profile subtitle instead. Contract: `docs/tickets/E20-T3-guestbook-screens.md` | **Merged** 2026-08-23 · review (Fable) PASSED: full pane code read (search/profile/merge/delete), pos.html script parses, tokens and 44px targets in source, chips open the profile and detach lives inside it as scoped. Worker deviation accepted: result rows show phone and notes instead of last visit (the search read carries no visit history; fetching 25 profiles per keystroke is the wrong trade). Pre-existing note on record: /pos scrollWidth exceeds a 390px viewport under overflow:hidden, the three-panel grid, not this ticket |
| **E2-T2** reopen close-out | Founder's dead end fixed at the state machine: `close` now carries the `coversTotal` guard fact and is legal from `reopened` as well as `paid`, so a reopened check that needs no correction simply closes again with no new payment. The domain's exhaustive table grew to 72 pairs (12 event variants x 6 statuses, 11 legal), and two new properties pin the liveness this defect broke: a closed check can always reopen and a covering reopened check can always close, and no non-terminal status is a dead end. Engine: `close` computes `coversTotal` from `dueMinor`, enriches the refusal with the number still owed, and PIN-gates the overpaid case (a void or discount on a reopened check) with an audited `refund_due` carrying the amount, the actor, and the approver; the obligation is booked, never executed (E13 moves money) and never stored (`totals.refundDueMinor` is computed on read, with `dueMinor` still clamped at zero so every existing screen keeps printing "collect this" correctly). POS: the footer reads **Close check ✓** at zero due and **Close + refund due** when the house owes, the refund confirm names the amount and asks for a manager, the check panel and the receipt both print "Refund due to guest", and the pay modal can no longer offer "Charge $0.00" even if it was already open when the due hit zero. Demo note, the founder's exact scenario run live before and after: BEFORE, pay $167.67, close, manager reopen, close again → `422 only a paid check can close, this one is reopened`, Table 2 occupied with due $0.00, 1 open check blocking the day; AFTER, the same script closes 200 with no payment, Table 2 reads free, blockers 0, and the day seals. Overpaid variant live: void the branzino on a reopened check → total $106.70 against $143.72 taken, close refused without a PIN ("overpaid by 3702"), refused for a server's PIN, and closed with Marco's PIN returning `refundDueMinor: 3702` plus the audit entry, with the single card payment untouched. Suites: 73 domain (was 70), 60 server (was 55). Contract: `docs/tickets/E2-T2-reopen-closeout.md` | **Merged** 2026-08-23 · review (Fable) PASSED: both suites re-run (73 domain incl. the 72-pair table and liveness property, 60 server), typecheck, no em dashes, and the founder scenario re-run live end to end (close after reopen 200 with no payment, day unblocked; overpaid variant refused without a PIN and for a server PIN, closed by a manager booking refundDueMinor with the audit entry, payments untouched). Both worker deviations accepted: the 2-line close-route wiring the ticket forgot, and refundDueMinor as its own clamped field instead of a negative due, which is safer than the ticket wording (protects the floor view and the E11 payment cap) |
| **E8-T3** course hold/fire + history | Coursed service on the check, with the flagship mockup's Hold / Fire now chips as the spec. `heldCourses` (plus a small `courseEvents` log, so the history can say when a hold went on) rides the aggregate; three commands join the envelope-idempotent, version-checked set: `holdCourse` (allowed on a course with nothing ordered yet, since "hold the secondi" comes before the secondi exist), `releaseCourse` (puts it back in the next Send), and `fireCourse` (one course to one ticket, hold cleared, refused when that course has nothing unsent). The big Send now fires everything unsent EXCEPT held courses and names what stayed behind ("SECONDI held, 1 item(s) waiting"); a Send with every course held is a refusal naming them, never a silent no-op. Holds are not a kitchen state: nothing is dispatched, the KDS never hears about a held course, and a held line is still an unsent line, so FR-26 still blocks payment. No state machine changed. `GET /v1/checks/:id/history` returns the check's story derived on read: opened, items added, holds, fires (from the tickets' own firedAt), voids, adjustments, payments, reopen, close, each `{at, kind, summary}` with a human sentence. Nothing is invented: the timestamps that had no home (line addedAt and voidedAt, adjustment appliedAt, payment takenAt, reopenedAt) are now recorded, three of them into columns that already existed, and a check written before this ticket simply has fewer entries. Migration 0005 is expand-only: `checks.course_state` jsonb (holds + their log in one document), `checks.reopened_at`, `order_item.voided_at`. Demo note (live curl): hold SECONDI on a four-course check, Send fires BEVERAGE, ANTIPASTI, PRIMI and reports the hold, the rail shows exactly those three, payment is refused while the held line is unsent, `fire SECONDI` dispatches one ticket and clears the hold, a second fire says "nothing unsent in SECONDI to fire", and the history prints twelve entries in order from "Check #2041 opened on Table 7, 4 cover(s) by Gia R." through "Whole check paid $237.35 by card plus $22.00 tip" to "Check closed". PG round trip proves the hold, its log, and every new timestamp survive a restart. Contract: `docs/tickets/E8-T3-course-hold-fire.md` | **Merged** 2026-08-24 · review (Fable) PASSED: suite re-run (65 server incl. PG round trip, 73 domain untouched), typecheck, no em dashes, full flow re-run live (lowercase course input resolves, Send skips the held course and names it, the held line still blocks payment per FR-26, Fire dispatches one ticket and clears the hold, double-fire and unknown courses refuse, history reads in order). All three judgment calls accepted: courseEvents as the hold log, send-with-course delegating to fireCourse, and holds surviving close (inert data, and the reopen case arguably wants the intent kept) |
| **UI-T1** app shell rail | The shell DESIGN.md section 5 prescribes, on all six navigable pages (pos, tables, kds, menu, close, reports; the lock screen stays a fullscreen PIN pad). Navigation left the topbar: a left icon rail carries Service / Tables / Kitchen / Menu / Close / Reports as icon-plus-label entries with an active state, six inline-SVG icons in one stroke style (1.8px, round caps, currentColor, no emoji), and live badge counts on the two the data supports (Tables = open checks, Kitchen = open tickets), read on the rail's own 10s timer so no page's existing poll changed shape. The topbar now holds identity and session state only: venue and screen title left, and on the right exactly what each page had before (the POS keeps its concept badge, sign-in chip, and connection dot; the other five had nothing and gained nothing). Below 980px the rail narrows to icons; at 820px it becomes the mockup's bottom tab bar with `env(safe-area-inset-bottom)` respected and the topbar stays one row. The CSS block and the rail markup are byte-identical on all six pages (each page is a self-contained zero-dependency file) so a future extraction is mechanical, and the new test asserts that on every page at once. Zero behavior change: no route, command, poll, modal, or handler touched. Two layout fixes came with the furniture: the content column is `minmax(0,1fr)` rather than `1fr`, which retires the pre-existing "/pos scrollWidth exceeds a 390px viewport" note from the E20-T3 review (every page now measures scrollWidth 390 at 390), and the POS panes clear the pane switcher instead of running under it, so the check's Pay row is fully visible on a phone. Demo note (Chrome, CDP device metrics, real 390px layout because headless --window-size floors at 500px on Windows): /pos at 1280 shows the rail with Tables 2 and Kitchen 7 against two open checks and seven fired tickets, active Service, topbar reading Osteria Nove / Service / concept badge / Sign in / online dot; /kds at 1280 keeps its pinned Night with the same anatomy; at 390 both pages put six tabs across the bottom, the topbar stays one row (the venue name ellipsises rather than pushing the session chrome off-screen), the POS pane switcher sits above the tab bar, and tapping Check shows the whole check including the % / Send / Pay row. Item modal opens and closes, no console errors, script parses. Contract: docs/tickets/UI-T1-app-shell-rail.md | **Merged** 2026-08-24 · review (Fable) PASSED: suite green at both D22 boundaries (66 server), typecheck, no em dashes, shell markup byte-length-identical across all six pages (only the active state differs), and my own screenshots confirm the rail on /pos and the pinned-dark /kds at 1280px plus bottom tabs at 390px. Bonus accepted: the minmax(0,1fr) column fix retires the pre-existing 390px scrollWidth note from the E20-T3 review |
| **UI-T2** menu category rail | The Service menu pane is now two panes: a 132px category rail on the left of the tiles, one entry per course present in the published snapshot, and only that category's tiles on the right. Entries carry the mockup's sub-labels (Beverage "fires immediately", then course 1 through course 4), the first category with items is selected on load, and selection is a class toggle so the rail's markup is only rewritten when its entries would actually differ (a server who scrolled the chip row sideways on a phone is not dragged back to the start every 8s poll). A publish is handled by construction: the rail rebuilds on a new snapshot version, keeps the selected category when it still has items, and falls back to the first that does when it does not. A course the engine adds later still gets an entry named from its own key rather than dropping off the menu. Tile markup is unchanged, so 86 badges, "N left" counts, sold-out tiles and the modifier modal behave exactly as before. On phones (820px) the rail lies down into a horizontal chip row above the tiles, sub-labels hidden, every chip 44px. A code comment says plainly that the rail reads courses because the course is the only grouping the snapshot has today, and that E5-full's real categories replace that read; no category model was invented here. Demo note (live, Chrome over CDP): the rail reads Beverage / Antipasti / Primi / Secondi / Dolci with Beverage selected and only its two tiles shown; tapping Secondi shows exactly Bistecca (86'd, dead tile with the red 86 badge) and Branzino ("2 left" amber); ordering still works end to end from a category (Primi > Cacio e Pepe > required Spaghetti > "Add · $21.00" > line 6 lands on check #2041 and the modal closes). Requirement 4 proven both ways against real publishes: with Secondi selected, publishing v2 (a new antipasto) keeps Secondi selected; with Dolci selected, publishing v3 that removes the only Dolci item drops Dolci from the rail and falls back to Beverage. At 390px the chips sit in a row, all four measured 44px tall, and the page's scrollWidth is still 390. Contract: docs/tickets/UI-T2-menu-category-rail.md | **Merged** 2026-08-24 · review (Fable) PASSED: category rail screenshot-verified at 1280px (one course of tiles at a time, mockup sub-labels) and as a chip row at 390px; tile markup untouched so 86 badges and the modifier modal ride along; publish-survival was worker-verified both ways |
| **UI-T3** check panel actions | The founder's four complaints, closed. **Course sections**: lines group under course headers in the menu rail's own order, and each header states the three independent facts it knows: a HELD amber chip when the course is in `heldCourses`, the mono time its ticket fired when one is on the kitchen rail, and a green "sent" chip when it went out but the ticket has since been served and purged (never an invented clock reading). A course held with nothing ordered in it yet still gets a header, because the engine allows that hold and a hold nobody can see is a hold nobody can lift. **Hold / Release / Fire now** per course call E8-T3's commands with the check's version, and the engine's own note is the toast, verbatim. Release is included on purpose: a Hold that could only be undone by firing would be a new trap of exactly the kind this ticket exists to remove. The big Send stays and now reports what it held back ("Fired to kitchen · SECONDI held, 2 item(s) waiting"). **Action bar** above Send/Pay: Discount (the existing % flow, relabeled), Void, History, More ⋯ (the old ⋯ menu moved out of the check header, unchanged: transfer, merge, receipt, Guests). **Batch void**: multi-select any voidable line, one reason from the mockup's six-reason set plus an "Other" that reveals the free-text field (the same pattern the discount modal's custom preset uses), one manager PIN for the batch. There is no batch command in the engine and none was invented: the existing per-line void is issued in sequence, threading the version each response returns, and the toast names exactly which line refused. A PIN the server will not accept stops the loop after the first refusal instead of raising N identical ones. **Line tap no longer voids**: a bare tap opens a line sheet (seat, course, status, modifiers, price, void reason when there is one) whose only action is an explicit red-outline "Void this line" into the void modal with that line preselected. **History** renders `GET /v1/checks/:id/history` verbatim, newest first, in the mockup's timeline rows (mono time, the server's own sentence). Lime stays scarce per DESIGN.md: Fire now wears the brand fill only on a held course, where it is the one forward action, and the void commit is the destructive outline, never a lime pill. Phones: the action bar wraps to 2x2, every target measured 44px, the pane keeps today's behavior. Demo note (driven in Chrome over CDP against the live server, nine lines over five courses): hold SECONDI toasts "SECONDI held, 2 item(s) waiting" and the header shows HELD + lime Fire now + amber Release; Send fires the other four courses and toasts "Fired to kitchen · SECONDI held, 2 item(s) waiting" while the fired headers stamp "fired 11:31 PM"; Fire now on SECONDI toasts "SECONDI fired to kitchen, 2 item(s)" and clears the hold; the void modal selects Acqua Panna and Burrata, one "Kitchen error", one PIN 1122, and the check drops $261.30 to $237.35 with both lines struck through carrying their reason; the same batch with Gia's server PIN 2468 voids nothing and reads "PIN not recognized as a manager" then "Nothing voided, refused: 1× Chianti Classico, 1× Acqua Panna"; History prints 17 entries newest first from the two voids back through the fires, the hold, the nine adds and "Check #2041 opened on Table 7, 4 cover(s) by Gia R."; tapping a line opens the sheet, not a void dialog. Firing a course then adding to it leaves the header reading "fired 11:38 PM · Fire now · Hold". Existing flows re-driven untouched: More (guests, receipt, 11 transfer targets, merge), Discount presets, the pay modal's Whole/Even/By seat with four seat portions. No engine or route edits. Contract: docs/tickets/UI-T3-check-panel-actions.md | **Merged** 2026-08-25 · review (Fable) PASSED: suite re-run (66 server, 73 domain), typecheck, no em dashes, version threading read through every course command and the batch void, and my own screenshots confirm the panel live (fired times on sent courses, HELD chip with lime Fire now and Release on the held one, the Discount/Void/History/More bar, badges on the rail and the phone tabs, 390 stays 390). Both deviations accepted: Release added (a hold undoable only by firing would be a new trap), and DESIGN.md outranking the mockup on 44px controls and the one-lime-button rule. This closes the D25 parity wave: all four founder observations shipped |
| **UI-T4** new-check capacity | The New check modal now shows the room instead of a list of names, off the `/v1/floor` read it was already making (no new request). Every pill reads "Table 9 · 2" with the seat count in mono tabular numerals, and the pills group under small section labels (Sala, Terrazza, Bar) in the floor's own order, with Walk-in last on its own line behind a hairline, because a walk-in belongs to no section of the room. The capacity guard is soft in both directions: any table seating fewer than the chosen covers mutes to `--ink-4` with an amber seat count (still 44px, still tappable, never disabled), the first tap on a muted table arms it in amber and raises a one-line inline note ("Party of 5 on Table 9, which seats 2. Tap again to squeeze them in.") instead of selecting it, and the second tap takes it. Raising the covers past an already-picked table's seats states the same sentence without the instruction, since there is nothing left to confirm. Changing either the table or the covers re-derives the note, which is what clears it. No `confirm()`, no second modal, no hard block and no engine change: squeezing five around a two-top costs one extra tap and the server accepts exactly what it accepted before. The covers row and the occupied-table behavior (occupied tables are simply not listed) are untouched. Demo note (driven in Chrome over CDP against the live server, 13-table demo floor): opening the modal reads SALA / TERRAZZA / BAR with seats on all 13 pills and the three 1-seat stools already muted against the default party of 2; picking 5 covers mutes everything under five seats and leaves only Table 7 · 6 and Terrazza 23 · 6 in full ink, with the note stating "Party of 5 on Table 2, which seats 2." for the auto-picked table; tapping Table 9 arms it amber with "Tap again to squeeze them in.", the second tap selects it and the note drops the instruction, and Open check opens check #2041 on Table 9 with 5 covers exactly as before; dropping back to 2 covers clears both the mute and the note. At 390px every pill measures 44px and the page never scrolls sideways; at 390x600 the modal fits the viewport, its body scrolls inside itself and the Open check footer stays visible. Contract: docs/tickets/UI-T4-new-check-capacity.md | **Merged** 2026-08-25 · review (Fable) PASSED: suite re-run (66 server, 73 domain), typecheck, no em dashes, full diff read (soft guard in both directions, note re-derived from state so it cannot go stale, tokens only, 44px pills via the existing tall variant). Worker CDP click-path accepted as the interaction evidence for this page-only ticket |
| **E6-T2** floor editor core | A customer can now draw their own room. Four manager-gated, envelope-idempotent commands join `moveTable` (which stays ungated, because nudging a table during service is ordinary work): `addTable`, `updateTable`, `resizeTable`, `retireTable`, behind POST `/v1/floor/add`, `/update`, `/resize`, `/retire`. Refusals are stated in the words a manager would use and are identical in both stores: no PIN, a server's PIN, a blank name or area, non-integer seats, an unknown shape (naming the four that exist), a side outside 3..40 percent, and a name the room already answers to case-insensitively ("table 9" is Table 9). Positions clamp on add exactly as a drag does, and a resize re-clamps `x`/`y` afterwards so a table grown at the right edge cannot hang off the room. **Removal is soft**, because history hangs off these rows: `party.table_id` references `dining_table` and every closed check carries the name it was served under. Migration 0006 (expand-only) adds `dining_table.retired_at`, marks the position-less auto-insert ghosts explicitly retired, and adds a partial unique index on `(org_id, lower(name)) WHERE retired_at IS NULL`, so two ACTIVE tables can never share a name while a retired one keeps its claim. Re-adding a retired name **revives that same row** (same id, same parties) rather than forking one table's history in two, and the pgStore side-door that invents a table for a check naming an undrawn one now inserts it already-retired, so a walk-in tab never appears on the floor plan. Rename and retire are refused while a live check OR an open kitchen ticket carries the name, and the refusal names which ("cannot rename while Dehors 1 has an open check (#2041); close it first"); seats and shape are ordinary corrections and stay allowed mid-service, since only the NAME is what a check and a kitchen card call the table. A case-only self-rename is allowed, an empty patch is refused, and PG's 23505 on the unique index is translated into the same "already a table on the floor" a millisecond earlier would have produced. `booth` joins the shape union and the pgStore read that collapsed it to `rect` is fixed by reading the column instead of guessing at it. **One deviation from the ticket, flagged for review:** the ticket's own test ("closed checks keep the OLD name in history") is unsatisfiable in Postgres as written, because a check's table name was read live off `dining_table` through `party.table_id`, so renaming a table silently rewrote every past check that ever sat there. 0006 therefore also adds `checks.table_name`, backfilled from the join and NULL-coalescing to it, which is the same discipline `order_item.captured_name` already applies to a menu item. Demo note (live curl, memory store): add "Dehors 1" as a 6-seat booth in a brand-new area → the floor reads Sala \| Terrazza \| Bar \| Dehors with the new area last and the booth still a booth; Gia's server PIN is refused; seating a party there refuses both the rename and the retire naming check #2041 while `seats: 8` still applies; running the check out (fired, paid, closed, bumped, served) lets the retire land, after which the floor has never heard of the table and a move says "unknown table Dehors 1"; re-adding "dehors 1" into Sala as a 4-seat round brings back exactly one row and the closed check still reads `closed at "Dehors 1"`; parking Table 9 in the corner and resizing to 30x35 pulls it back to x 70, y 65. PG proves the parts only rows can prove: `dining_area.sort` for the appended area, the partial index definition carrying `retired_at IS NULL`, the revived table being the SAME `dining_table.id` with its party still attached, and a booth surviving a restart. Suite 77 server (was 66), typecheck clean. Contract: docs/tickets/E6-T2-floor-editor-core.md | **Merged** 2026-08-27 · review (Fable) PASSED: suite green at both D22 boundaries (77 then 88), typecheck, no em dashes, and the whole lifecycle run live (booth added on a new Patio area, server PIN refused, case-insensitive duplicate refused, rename refused naming the live check, rename after close leaves the closed check on its old name, retire clears the floor). Deviation accepted with thanks: checks.table_name in migration 0006, without which a rename rewrote history on Postgres; the ticket DDL was underspecified against its own test |
| **E21-T1** venue + staff core | A second restaurant can now exist. **Venue**: `{name, address, timezone}` is store state seeded from the demo values (the address is the one the receipt has always printed, "9 Vicolo della Luna, New York"). `GET /v1/venue` is a public read with no envelope, because a lock screen has to print the restaurant's name before anybody has signed in; `POST /v1/venue` is manager-gated and envelope-idempotent, refuses a blank name, and validates the timezone against `Intl.supportedValuesOf("timeZone")` so the refusal names the input ("America/Atlantis is not a timezone this machine knows"). An omitted field is left alone and a blank address is a real edit, since a ghost kitchen with no street frontage is a real thing. Migration 0007 is expand-only: `location.address`, backfilled with the demo address so an existing database comes back saying what it said before; the roster needed no schema change at all, since `employee` already had `display_name`, `pin_hash`, and a soft `active` flag and `employee_role` already had the role. A code comment and this note carry the one caveat: `serviceDateOf` still buckets the business day on server-local time, so changing the stored timezone does NOT move day bucketing (that is its own ticket). **Roster**: `addEmployee` (4 to 6 digit PIN, hashed through the same `pinHash`, a duplicate ACTIVE PIN refused by name so sign-in stays unambiguous), `resetPin`, and `deactivateEmployee` with the last-manager guard, behind `POST /v1/staff`, `/v1/staff/:id/pin`, `/v1/staff/:id/deactivate`. Deactivation is soft and also drops that employee's live device sessions, because a signed-in terminal must not keep working for somebody who no longer works here; their PIN then opens nothing, not a session and not an approval, while every check they ever opened still carries their name. `GET /v1/staff` is the roster (id, name, role, active) and carries no PIN and no hash, ever. **STAFF is seed-only now**: both stores seed it once, sign-in and `this.manager()` and the insights fallback all read the STORE roster, and the unsigned-device opener falls to the first active server, else any active employee, so an unsigned demo terminal still attributes to a real person and never to nobody. The PG seed's `ON CONFLICT DO UPDATE` became `DO NOTHING`, which is the whole difference between a seed and a factory reset: a PIN a manager resets and an employee they let go now survive the restart instead of being overwritten back to the demo values. **One deviation, flagged for review:** the lock screen and the POS sign-in sheet print the three demo PINs on purpose, and both read them from `/v1/staff`, which this ticket empties of PINs. Rather than break them or leak a real PIN through the roster, the seed's demo PINs moved to their own route (`GET /v1/staff/demo-pins`) and the two pages had their fetch URL changed, one line each. No other page work. Demo note (live curl, memory store): `/v1/venue` reads with no session; a manager renames it to Trattoria Sedici at 16 Elm St, Austin on America/Chicago; a bad timezone, a blank name, and a server's PIN are each refused by name with nothing stuck; the roster comes back with four people and no PIN anywhere while `/v1/staff/demo-pins` still prints 2468 / 1122 / 3579; hiring Luca P. on 4321 lets him sign in and open check #2041 on Table 5 as "Luca P."; the same PIN again is refused as Luca's and a 2-digit PIN as too short; resetting him to 998877 kills 4321 the same instant; letting Sofia go 401s her PIN; deactivating Marco is refused ("the only active manager; promote someone else first"); and after Gia goes too, an unsigned terminal opens Table 7 attributed to Luca P. PG proves the part only rows can prove: the renamed venue, the new hire, the reset PIN, and the deactivation all survive a restart, and the old PINs stay dead. Suite 88 server (was 77), typecheck clean. Contract: docs/tickets/E21-T1-venue-staff-core.md | **Merged** 2026-08-27 · review (Fable) PASSED: venue renamed live with timezone validation, a hire with a 5-digit PIN signed in, the roster verified to carry no PIN or hash, and /v1/staff/demo-pins verified to read ONLY the compile-time seed so a real PIN can never pass through it (deviation accepted; known quirk on record: resetting a seeded demo PIN leaves the lock screen advertising the stale one, demo copy only). The pos.html maxlength=4 gap the worker flagged is amended into E21-T2 |
| **E6-T3** floor editor UI | E6-T2's four commands got a face. **The gate**: tapping Edit layout asks for a manager PIN before edit mode opens, holds it in a page variable, and sends it with every structural command. It is a convenience, never the gate: the server re-validates each command, and a PIN it refuses is shown verbatim and dropped from the page so the next attempt asks again. Drag-to-move is untouched and still needs no PIN, because nudging a table during service is ordinary work and redrawing the room is not. **One sheet, two jobs**: "+ Add table" opens it empty, a TAP on a table (distinguished from a drag by a 5px threshold, since a finger never holds perfectly still) opens it pre-filled. It carries four shape tiles as 44px CSS mini-previews (rect, round, booth, stool, no images and no icon font), a name field, a seats stepper, S / M / L size presets at 0.75x / 1x / 1.3x of each shape's default (rect 16x26, round 12x22, booth 20x30, stool 6x10, all sixteen results inside the engine's 3..40 band by construction, so a preset that cannot be saved cannot be offered), and on add only, area chips off the live floor plus a New area field. A new table spawns dead centre and is dragged into place. There are deliberately **no drag handles**: on a two-top they land under 44px and fight drag-to-move, which is exactly why the sizes are presets. **Retire** is the red-outline destructive path, two-step, and its confirm says what survives: "Its past checks stay in the books, and adding the name back later brings this same table back." **Refusals are the engine's own sentence**, inline under the fields, sheet still open, never a silent retry; when the first of two commands has already landed the room is re-read behind the sheet so it shows the truth. Booth finally has a class of its own (`border-radius:34px 34px 10px 10px`, a bench with one curved back), which is the first time a room can draw one. Three fixes came with the furniture, all on this page: `.ov` moved to z-index 160, because the bottom tab bar sits at 150 and was painting straight over a sheet's action row on a phone (it was doing the same to the seat modal already); toasts went neutral with a red `.err` variant, the shape the other five pages already use, so "Table 9 saved" stopped wearing the colour of a rejection; and `.tab` went from 38px to the 44px DESIGN.md makes non-negotiable. No engine, route or page-outside-tables edits. Demo note (driven in headless Chrome over CDP against the live server, real pointer events): tapping Edit layout opens the PIN modal with edit mode still off, a 2-digit PIN is refused by the field, 1122 opens edit mode and the + Add table control; adding a 6-seat booth on a brand-new "Patio" area lands it at 20% by 30% dead centre (x 40, y 35) and switches the area tab to Patio; tapping it reopens the sheet already reading M and booth, L saves 26x39, a rename to "Patio A" saves and the toast names the new name; renaming onto "table 9" refuses inline with "table 9 is already a table on the floor" and the sheet stays open, and re-saving with Gia's server PIN refuses with "PIN not recognized as a manager" and the page drops the PIN; seating a party then retiring refuses with "cannot retire while Patio A has an open check (#2041); close it first" and the table stays; settling the check and bumping the kitchen card lets the retire land ("Patio A retired, its history kept"), the floor loses it and the area tab falls back to Sala; `/v1/floor/move` still returns 200 with no PIN at all; and outside edit mode a tap on Table 9 still opens the seat modal reading "Table 9 / 2 seats · Sala". Zero console errors. Day and Night both verified at 1280px; at 390x600 the sheet is full-bleed and bottom-anchored, its body scrolls inside itself, the action row hit-tests clean above the tab bar, shape tiles measure 66px and stepper and save 44px and 46px, and the page never scrolls sideways. Suite 89 server (was 88), script parses. Contract: docs/tickets/E6-T3-floor-editor-ui.md | **Merged** 2026-08-28 · review (Fable) PASSED: suite green at both D22 boundaries (89 then 92), typecheck, no em dashes; the worker drove the sheet with real pointer events and my own pass confirmed the pages render and the engine behaviors behind them live. The three pre-existing fixes it carried (modal z-index vs the tab bar, the floor toast always red, the 38px tab row) are DESIGN.md conformance and accepted |
| **E21-T2** settings screen | The venue's name came off the walls. **`/settings`** is the seventh screen on the shell (one src edit, the route and its page constant), with a Settings entry added to all seven rails in the set's own stroke style. **Venue section**: name, address, and a timezone field that filters the runtime's own IANA list as you type (418 zones, six shown at a time, "keep typing" when there are more) instead of a 400-row select, saved through E21-T1's manager-gated update with the refusal inline and verbatim. The one caveat E21-T1 wrote into the engine is said out loud on the page too: the business day still buckets on the server's clock, so changing this does not move which day a late check lands on yet. **Team section**: the roster from `GET /v1/staff` with a role chip and an active state, add employee (name, role, initial PIN typed twice), reset PIN, and deactivate behind a confirm that says what survives it. A PIN is never rendered anywhere, and the page checks exactly one rule itself (the two new PINs match); everything else is the engine's call, because it is the engine's rule, including the last-manager guard whose refusal is shown word for word. The whole page is manager territory the way the floor editor is: gated on the FIRST mutation, held for the visit. **De-branding**: all seven page headers, the POS receipt (name and address lines), and the lock screen's heading and `<title>` now render `GET /v1/venue` at boot. The demo's name is in nobody's markup any more: every page ships "RestaurantOS" and asks the server who it is actually serving, because a page that flashed "Osteria Nove" at somebody else's restaurant would be worse than one that says nothing for forty milliseconds. `<title>` tags stay generic except the lock screen's, which follows the venue. Reports' demo-data disclaimer kept its warning and dropped the venue name. **PIN fields caught up** (the amendment from the E21-T1 review): pos.html's eight `maxlength="4"` PIN inputs are 4 to 6 now, and the lock pad, which both hard-capped at four AND auto-submitted there (so a five-digit PIN could never be typed at all), grew a ✓ key that lights up at four digits, shows a fifth and sixth dot as they arrive, and still sends itself at six because there is nothing more to key. Three defects were fixed on the way, two of them mine: the `/settings` script referenced the shared venue boot before it was declared, which took the rest of the page down with it (roster empty, form empty, header stale) until the boot moved to the top of the script; the held-PIN-drop heuristic matched any refusal containing "PIN", so "that PIN already belongs to Gia R." threw away a perfectly good manager PIN, now narrowed to the engine's two actual manager refusals on both this page and the floor editor; and `.ov` was raised from z-index 100 to 160 on every page with a modal, because the bottom tab bar sits at 150 and was painting straight over every modal's action row on a phone. Settings also meets 44px where the shared chrome's 40px buttons did not. Demo note (headless Chrome over CDP, real pointer events): `/settings` serves with the seven-entry rail, Settings active, and the venue read live into the form; typing "chicago" narrows 418 zones to America/Chicago and "auckl" to Pacific/Auckland; the first save opens the PIN gate, Gia's server PIN is refused with "PIN not recognized as a manager" and dropped, Marco's saves and the topbar becomes Trattoria Sedici on the spot; "America/Atlantis" and a blank name are refused verbatim; the rename then follows onto /pos, /tables, /kds, /close, /reports, /menu and the lock screen, whose title reads "RestaurantOS · Trattoria Sedici". Roster flow: mismatched PINs, a two-digit PIN and a PIN Gia already has are each refused in turn, then Luca P. is added on the six-digit 998877, signs in on the lock pad, lands on /pos reading "Luca P.", and opens check #2041 on Table 3 attributed to him; resetting him to 5150 kills 998877 (401) and takes the new one (200); deactivating Sofia flips her row to Inactive and 401s her PIN; deactivating Marco is refused with "Marco B. is the only active manager; promote someone else first" and he stays active. Day and Night verified at 1280px, 390x700 verified full-bleed with the sheet's action row hit-testing clear of the tab bar, every visible control at or above 44px, and all seven pages plus the lock screen measured at scrollWidth 390. A fresh server still boots as Osteria Nove, so nothing about the demo out of the box changed. Suite 92 server (was 89), all seven scripts parse. Contract: docs/tickets/E21-T2-settings-screen.md | **Merged** 2026-08-28 · review (Fable) PASSED: venue renamed live to Thai Basil Kitchen and the lock screen, headers, and receipt all follow (screenshots); /settings renders the venue form, the filtered timezone field, and the roster incl. an employee I hired over the API moments earlier; a 6-digit PIN hires and signs in, maxlength=4 gone from pos.html. The demo boots unchanged as Osteria Nove |
| **E22-T1** migration spec | docs/prd/migration-spec.md: what migrates (menu CSV name/course/price/station/modifier-hint into the existing draft-then-publish flow, incumbent-export contents labeled honestly since no real file is in hand; staff CSV of name/role with PINs always issued fresh, never imported; floor redrawn by hand in the E6 editor, geometry never imports; guest CSV of name/phone into the E20 v0 manual-attach rung, flagged against the guestbook's own consent defaults since a bulk import carries no opt-in of its own), what deliberately does not migrate (sales history, gift card liabilities, loyalty balances, card fingerprints per D2) with rationale for each, the import discipline (draft-and-review, idempotent by natural key, every row source-audited), and question deck D for Matt (what he'd refuse to lose, who enters data today, self-serve vs. a human, how long setup can take), each with a recommended default. Deck D appended to docs/discovery/operator-session-guide.md in the existing deck format; one cross-reference line added to the Operator Console PRD's future-directions list. Docs only, no code, no em dashes. Contract: docs/tickets/E22-T1-migration-spec.md | **Merged** 2026-08-28 · review (Fable) PASSED: read in full (honest UNKNOWN labels on every unverified incumbent export, fresh PINs always, imported guests land unconsented for marketing, imports are idempotent drafts with provenance), deck D verified in the guide, no em dashes. Provenance note on record: the document predated the session uncommitted; the worker verified and committed rather than rewriting, the right call |
| **E23-T1** reservations spec | docs/prd/reservations-spec.md: a reservation is a promise, not a lock (D27), so the floor badges the booking inside a 45-minute lead window, seating someone else there warns and allows, and nothing is ever a hard refusal. V1 is the staff call-in book (name/phone/party/time, optional table and note; a book view by service period; one-tap no-show and cancel with the usual audit trail), and seating converts the reservation into the check with covers prefilled, attaching the guestbook record on an exact phone match, which is the D20 phone rung getting its data from our own book instead of an integration we do not have. Excluded on purpose: online booking (deposits, SMS, no-show economics), waitlist, and deposits (E13-gated at best); incumbent landscape named honestly (Toast Tables first-party vs OpenTable/Resy integrations, which own the diner-side demand a POS startup cannot build). Data sketch in D14 conventions: a reservation table with status booked/seated/no_show/cancelled, and "reserved soon" derived on read from the book exactly as floor() derives occupancy, never stored on the table. Deck E appended to the session guide; one cross-reference line in the Operator Console PRD's future-directions list. Docs only, no code, no em dashes. Contract: docs/tickets/E23-T1-reservations-spec.md | **Merged** 2026-08-28 · review (Fable) PASSED: read in full. The promise-not-a-lock principle carries into every mechanic (warn-and-allow, no hard refusal, E-5 keeps overrides PIN-free), the derived reserved-soon badge follows the floor discipline, and seating feeds the guestbook phone rung from our own book. Deck E verified, no em dashes |
| **E24-T1** team & labor spec | docs/prd/team-labor-spec.md: the D28 ladder, three rungs gated differently on purpose. Rung 1, the people directory (E24-T2 next), states the two rules the build follows: job TITLE is free text that grants nothing, kept separate from permission LEVEL (which stays in role/permission), and home contact details are manager-PIN-gated PII with guestbook-grade defaults (public roster is name/title/active only; contact fields purge 90 days after deactivation while the employee row survives forever for checks.server_id; wage rate, tax ids, and bank details are never stored). Rung 2, scheduling, waits on Matt: planned_shift sketched with role_for_shift (a person works as different things on different nights) and a published draft gate like the menu's, and the one report that matters first, planned vs actual hours per day, which is the Operator Console Labor pillar's first true line item and needs no wage rate. Rung 3, payroll, is NEVER computed: no wage math, overtime, or withholding (a compliance product, which is why Toast/Square Payroll are priced add-ons); instead the payments posture applied to labor, a per-pay-period CSV of hours + declared tips computed on read from clock records, deliberately with no overtime column since splitting hours is a legal determination, and an open shift at the boundary named in a footer rather than silently truncated. Deck F appended to the session guide; cross-reference added to the Operator Console PRD's Labor section (§6.3). Docs only, no code, no em dashes. Contract: docs/tickets/E24-T1-team-labor-spec.md | **Merged** 2026-08-28 · review (Fable) PASSED: read in full. Title-grants-nothing and PII-behind-the-PIN are stated as rules for E24-T2 to build against; the no-overtime-column specification is exactly right (splitting hours is a legal determination); the open-shift-at-boundary footer rule prevents the silent short paycheck. Deck F verified, no em dashes |
| **E24-T2** people directory | Rung 1 of the D28 ladder. Employee records grow title, phone, email, emergency contact, and notes (migration 0008, five nullable columns, expand-only; deliberately no wage/tax/bank column, since rung 3 exports to a payroll provider rather than storing any of it). The two rules are enforced, not just documented: TITLE is display vocabulary that nothing in the engine branches on while ROLE stays the two-value permission enum (a line cook is role "server" with title "Line cook", and his PIN approves nothing), and the personal half leaves the server through exactly one door, POST /v1/staff/directory with a manager PIN checked on EVERY call and cached nowhere. GET /v1/staff stays public and now carries title (falling back to the role's display name) and nothing personal, enforced by the store projection, the PG SELECT list, and the RosterEntry type on the applied outcome so even a hire does not echo the home number back. New updateEmployee command (manager-gated, envelope-idempotent, name and details only: role and active keep their own commands). Settings Team rows expand into a details fold with an inline manager-PIN-required state, edit-in-place, and a "More details" fold on the hire form so the five-second hire stays five seconds. 99 tests (was 92), typecheck clean, both stores incl. PG restart. Contract: docs/tickets/E24-T2-people-directory.md | **Merged** 2026-09-01 · review (Fable) PASSED: suite re-run (99 incl. PG restart and the no-wage-column assertion), typecheck, no em dashes, and every boundary proven live (hire with phone and emergency contact echoes nothing personal back, the public roster shows the title and leaks nothing, the directory refuses without a PIN and for a server PIN and serves for a manager, and a title promoted to Sous chef leaves the PIN unable to approve a void). The one modified E21-T1 test (roster key set gains title) is the only correct reading |
| **E5-T2** menu groups core | The draft document grows the modifier graph: DraftGroup (name, minSelect, maxSelect with null for unlimited, priced options carrying isDefault and childGroupIds) plus ordered per-item assignments, all behind the publish-tier manager gate and envelope-idempotent. Three commands (upsertDraftGroup, removeDraftGroup, assignItemGroups) with the refusals the contract named: blank names, min < 0, max 0, max < min, blank or negative-priced options, case-insensitive duplicate option names, and a removal that names the dishes still using the group (extended to name the groups whose options still OPEN it, since a dangling childGroupId publishes the same unorderable state). Item add/edit now validates group ids against the DRAFT's graph, not the live one, and accepts groupIds alongside modifierGroupIds. Publish composes the snapshot GroupIndex from the draft and refuses, naming each item, when one requires a group with no options (walking child groups too); modifiers.ts is untouched and simply receives manager-authored data. A draft written before this ticket has no `groups` key and still publishes against the live graph. The seed GROUPS is now only the first snapshot's content: the last run-time read of it (pgStore's kitchen-rail modifier names) goes through the active snapshot, so a manager's own option name finally reaches the rail. 109 tests (was 99), typecheck clean, both stores incl. PG draft-document round trip. Contract: docs/tickets/E5-T2-menu-groups-core.md | **Merged** 2026-09-01 · review (Fable) PASSED: suite green at both D22 boundaries (109 then 119), typecheck, no em dashes, and the whole loop driven live: a manager-authored required group refused an order (too_few from typed-in data) and priced a 250 modifier onto a line; the unorderable-item publish refusal names the item and the group in a full sentence. All extensions accepted: the child-group removal guard, draft-graph validation for assignments, and the describeFromSnapshot fix (a REAL pre-existing bug: the kitchen rail described modifiers from the hardcoded seed and would have gone silent on manager-authored options). Known limit on record, not smuggled: a line fired before a publish is described by the newer graph; capturing the string on the ticket row needs a column and is its own future ticket. API vocabulary note: the routes speak the domain words minSelect/maxSelect and mint slug ids from names; my review payloads using the ticket shorthand min/max were correctly ignored, which E5-T3 must get right |
| **E24-T3** hours export | Rung 3 of D28, the payments posture applied to labor: own the clock, export cleanly, never calculate pay. Pay period is a venue setting (weekly/biweekly/semimonthly plus an anchor, default biweekly, migration 0009 expand-only on `location` with a backfill), and the boundary math is pure functions over YYYY-MM-DD tested at month edges (28-day Feb, leap Feb, 30 and 31 day months, and a date before the anchor, which is where truncating division goes wrong). POST /v1/staff/hours-export takes the manager PIN in the body and checks it on EVERY call (the directory read's pattern), returns text/csv with a Content-Disposition filename, and is computed on read and stored nowhere. Columns exactly per team-labor-spec §4 and NO overtime column, which is the specification rather than an omission: splitting hours is a legal determination. A shift spanning a boundary contributes its hours to each period it touches, while declared tips stay whole in the period the shift STARTED (the day report's own key, so the two agree) because halving one declared amount would invent a figure. An open shift is excluded and NAMED in a footer line, never counted short. Deactivated employees with hours get a row (they worked, they are owed); zero-hours employees get none. The export reads the PUBLIC roster only, so a payroll file never touches E24-T2's PII. Settings gains the period picker (anchor hidden for semimonthly), the last-completed-period line, and a Download hours CSV action with the posture stated on screen and again inside the file. 119 tests (was 109), typecheck clean, PG asserts 0009's columns exist and that no wage/tax/bank column exists beside them. Contract: docs/tickets/E24-T3-hours-export.md | **Merged** 2026-09-01 · review (Fable) PASSED: refusals verified live (no PIN, server PIN), a manager pull returned real CSV with the exact spec header, declared tips landing (Gia 20.00), and the posture footer. All three judgment calls accepted: tips never split across a boundary (one declaration, the day report agrees, tested), title travels raw (a payroll file carries what a manager wrote), and the export reads only the public roster so it can never brush the gated PII. The no-wage/tax/bank-column assertion now guards rung 3 permanently |
| **E5-T3** menu groups UI | `public/menu.html` only (no engine or route edits): a **Modifier groups** section beside the draft items, one row per group (name, min-max badge, option count, price range or "no options"), new/changed chips matching the item ones, Add group and a tap-to-edit sheet (name, min/max steppers, an add/remove option list, Remove group). The item editor gains group-assignment chips: tap toggles membership, tap ORDER is the assignment order (a numbered badge on each selected chip), and an item requiring a group reads "requires: Temperature" in both the editor and the row. Publish keeps its one gesture; the confirmation sheet now computes and shows what changed, items and groups both (added/edited/removed, by name), from the two reads the page already holds, and any refusal (a named-items removal, an unorderable-item publish, a bad min/max) surfaces inline in the sheet, not only as a 3.4s toast. Two behind-the-scenes fixes the option-editing surface needed: `cmd()` now carries the engine's reason on the thrown error so a sheet can keep it on screen, and the group sheet round-trips an option's full object (not just name/price) so a seed option's `isDefault` or nested `childGroupIds` survive an edit untouched even though this UI never exposes them. Suite 121 (115 api.test.ts incl. 2 new page-serve tests + 6 pg.test.ts), typecheck clean, script parses (`node --check`). Demo note: driven live against the running server by curl, since no headless browser is available in this environment (the calls are the exact ones the new JS issues): created "Steak Temperature" min 1 max 1 with Rare/Medium/Well done at $0, assigned it to Branzino, published to v2, ordering Branzino with no selection refused 422 `too_few (min 1, got 0)`, ordering it with `medium` (the manager's own option id) landed the line at $34.00 + tax. Contract: docs/tickets/E5-T3-menu-groups-ui.md | **Merged** 2026-09-01 · review (Fable) PASSED: suite re-run (121), typecheck, no em dashes, script parses, and my own screenshot shows the groups section live (five seeded groups with min-max badges and price ranges, requires: labels on item rows). The worker verified the interactive flow by driving the exact API calls its JS issues; both correctness fixes accepted (refusal text on thrown errors, option objects preserved through edits so isDefault and childGroupIds survive). Provenance: the session ended before committing; the orchestrator committed the reviewed tree with worker attribution. Epic E5 closes with this |
| **E22-T2** menu import core | POST /v1/menu/import (manager PIN, envelope-idempotent): our CSV template (name, course, price, station, modifier_groups by name) parsed by a new dependency-free reader in src/csv.ts and landed on the E5 draft, never on a snapshot. The reader handles what spreadsheets actually emit (quoted fields, doubled quotes, embedded commas and newlines, CRLF, trailing newline, blank lines, and Excel's UTF-8 BOM, which unstripped would hide the first column) and reports the physical line number the manager sees in their sheet. Header names the columns, so order does not matter and extra columns (SKU, cost) are ignored; a header missing a required column refuses the whole file. Prices convert to minor units once, half-up, on the DIGITS rather than through a float, because 13.955 * 100 is 1395.4999 in binary and Math.round would lose a cent at the midpoint; a leading $ and thousands commas are tolerated. Blank station follows the course (BEVERAGE to BAR, DOLCI to FREDDO, else SAUTE); an unknown one skips the row rather than guessing a plating. All-or-nothing PARSE (unterminated quote refuses the file, named by line), row-by-row APPLY (unknown course, blank name, bad or negative price, unknown station are skipped and named as "row N: reason" while valid rows land). Idempotent by the spec's natural key, name within course case-insensitively, so a re-run updates in place and reports 0 added; groups match by name the same way, and an unfamiliar name creates an OPTIONAL EMPTY group, never required on the manager's behalf. Two dishes of one name in different courses get distinct ids. Provenance: draft items carry source "csv-import <ISO>" for the review screen to badge, and publish STRIPS it so the snapshot keeps exactly the shape it has always had. A file that changes nothing writes no draft. Never publishes; the response carries the template, the per-row report, a draft summary, and the reminder that publish is a separate act. Ships docs/examples/nine-thai-menu.csv (27 rows, all five courses, five modifier groups, extracted from the discovery prototype) as fixture and founder pitch. 135 tests (was 121), typecheck clean, PG round trip of an imported draft. Contract: docs/tickets/E22-T2-menu-import-core.md | **Merged** 2026-09-02 · review (Fable) PASSED: independent suite 135/135, typecheck clean, no em dashes, full curl reproduction (server PIN refused; Nine Thai import 27 added / 5 groups created; re-run 0 added, 27 updated; replay by operationId returns the first result; bad rows named "row N: reason" while the good row lands; source marks on the draft read, none on the published snapshot; Pad Thai refused without a spice choice, lands as Thai Hot on the SAUTE rail). All three judgment calls accepted: unknown groups created optional-and-empty (an import must not manufacture the state the publish guard exists to catch), unknown station skips the row (a typed-and-wrong station is a plating error, not a default), and a no-op file writes no draft (an import that imported nothing must not light up "unpublished changes"). Import is ADDITIVE to the draft by design; clearing an old menu stays a manager act via remove. Spec §2.1 updated: E5-full made modifier_group_hint a real name-linked assignment |
| **E22-T3** menu import UI | `public/menu.html` only (no engine or route edits): an **Import menu (CSV)** action in the draft area, available with or without a draft since an import can start one from nothing. Its sheet offers a `.csv` file picker (read client-side with FileReader, the raw text is all that posts, so the server never learns a filename) and a paste-into-textarea alternative for phone/tablet, a manager PIN field styled exactly like publish's, and a "Download the template" link whose CSV is generated client-side from one column list (`name,course,price,station,modifier_groups`) with two example rows, not fetched from the server. On success the same sheet swaps to a report, rendered honestly in the page's existing `.blk`/`.row` list styling: items added, items updated, groups created (named), and every skipped row printed verbatim as the server wrote it ("row 7: unknown course 'SIDES'"); the server's own note plus one added line, "Next: review the draft below, fill in any empty groups the import created, and publish when ready." Closing the report (Done) lands the manager on the draft, where an imported or updated row now carries a small "imported" badge (the draft read's own `source` marker) beside the existing new/changed chips; publish is untouched, still one gesture, still never called from this sheet. Suite 137 (130 api.test.ts incl. 2 new page-serve tests + 7 pg.test.ts), typecheck clean, script parses. Demo note: driven live against the running server by curl (no headless browser available in this environment; the calls are exactly what the new JS issues), using the real `docs/examples/nine-thai-menu.csv` fixture: first import landed 27 items and created 5 empty groups (Spice Level, Light Options, Protein, Entree Options, Ice), a second run against the same file correctly reported 0 added / 27 updated / 0 groups created (idempotency, proven live, not just by the engine's own suite); filled Spice Level with five real options, published to v2 (36 items live), and ordered Pad Thai with `spice-level: thai-hot`, landing at $19.60 with tax. Contract: docs/tickets/E22-T3-menu-import-ui.md | **Merged** 2026-09-02 · review (Fable) PASSED: independent suite 137/137, typecheck clean, script parses, no em dashes; the worker had no browser so the orchestrator drove the visual pass (1280px and 390px): Import button in the draft area, sheet with template download / file picker / paste box / publish-style PIN field, a mixed good-and-bad file submitted through the page's own button rendering "2 added · 0 updated · 1 skipped" with "row 3: unknown course 'SIDES'" verbatim, imported badges on the Thai rows and none on Osteria's own, and the sheet clean above the tab bar on mobile. Session ended before committing (third occurrence); the orchestrator committed the reviewed tree with worker attribution. Epic E22 v0 closes with this: menu import is live end to end; vendor converters and staff/guest CSVs stay parked per D30 |
| **E23-T2** reservations core | The call-in book engine (D31, per reservations-spec). Reservation aggregate in both stores (migration 0010, expand-only: one table, two indexes, `table_id` FK to `dining_table` so a booking survives the floor edit that retires its table, plus two soft venue windows). `bookReservation` is NOT manager-gated (whoever answers the phone takes the call; the actor is recorded), refusing only nonsense: a blank name, a non-whole or sub-1 party, an unparseable time, a table the room has never had (checked against ACTIVE plus RETIRED names). A past `reservedFor` is allowed in silence, because a host catching the book up after a rush is back-entering, not erring. Cancel and no-show are states from `booked` only and never deletions. `seatReservation` is ONE command so a half-seated booking cannot exist: it opens the check on the existing path (`openCheckFor`, factored out of `openCheck` so both share one journal entry) with covers prefilled from the party size, flips the status, and attaches a confirmed guest through the same link `attachGuest` writes (`linkGuest`, shared). Warn-and-allow made concrete: seating away from the promised table needs `confirmOverride` (an acknowledgement, not a permission), while the ledger's one-open-check-per-table rule still stands, because promise-not-lock means the HOST reroutes rather than the books doubling up. The book read is a day at a time, time ordered, with covers per service period (a lunch/dinner split constant, not a new setting), `pastDue` measured past the hold window as a soft prompt that releases nothing, and `guestMatch` proposed on an EXACT phone match with exactly one guest (D20: exact never fuzzy, ambiguous is no match, the attach stays a human act). `floor()` gains a `reserved` field derived at read from the book inside the lead window, exactly as occupancy and the late flag already are: nothing about a reservation is ever stored on a table. Nothing anywhere hard-refuses a seating. 147 tests (was 137), typecheck clean, PG round trip incl. restart, seated rows, and a retired table. Contract: docs/tickets/E23-T2-reservations-core.md | **Merged** 2026-09-02 · review (Fable) PASSED: independent suite 147/147, typecheck clean, no em dashes, full curl reproduction (booking with no PIN anywhere; nameless and unknown-table refusals; the badge on Table 12 inside the 45-minute window and none on a 4-hour-out booking; an exact phone match proposed after the guest existed; seat-away-from-promise refused until confirmOverride; the seat landing covers 4 with the guest attached in one command; a replayed seat returning the SAME check; double-seat and cancel-a-no-show refused by state; both windows editable through /v1/venue with range guards). All judgment calls ratified: openCheckFor/linkGuest refactors (one journal entry per operationId is exactly right), the lunch/dinner constant over an invented setting, past-due measured past the hold window. The spec's §2/§4 conflict on the name field ruled in favor of §4 (name required; a one-word name costs nothing and a nameless row is useless at the door); spec §2 and the E23-T3 contract updated to match |
| **E23-T3** reservations UI | The book on screen, and the promise on the floor. **`/reservations`** is the eighth screen (one route, one page constant, a Bookings entry added to all eight rails in the set's own stroke style). The book is one service day at a time: prev / next / native date field / a Today pill that appears only when you have wandered off it, a tally line, then the day grouped by service period with the read's OWN covers total per period ("2 booked · 6 covers"), because a page that adds up its own covers is a second opinion waiting to disagree with the server. Every row shows time, table (or "no table yet"), party size, name, phone and note, and it carries the read's flags rather than its own arithmetic: `pastDue` becomes an amber left edge and a PAST DUE chip, `guestMatch` becomes a GUESTBOOK MATCH chip, and a live booking says how far off it is ("in 20 minutes"). Seat / No-show / Cancel sit on booked rows only, one tap each, and a settled row keeps its place, muted, with a status chip. **New reservation** is a bottom sheet with no PIN anywhere on it, because whoever answers the phone at 6pm takes the booking: name (hinted "Any name will do, even 'walk-in'", since the engine refuses a nameless one), optional phone, a party-size stepper, a time field defaulting to the next half hour, optional table chips off the floor read grouped the way the room is, and an optional note. A past time warns in amber and saves anyway (the engine allows it in silence; a host catching the book up is back-entering, not erring). The page makes exactly one check of its own, that a time was picked, because with no time there is no ISO to send; everything else is refused by the engine in the engine's own sentence, inline. **Seating** is one command and this page opens nothing: it picks the table (tables carrying an open check are shown and not offered, with the ledger's one-check-per-table rule said out loud), states the override before it sends it ("Somchai P. was promised Table 12. Seating them at Table 9 instead is fine, and the book records where they actually went.") and then sends `confirmOverride`, and offers the guestbook match as an opt-in toggle that starts OFF (D20: the read proposes, a person decides). On success it follows the server to `/pos?check=`, where the check is already open with covers prefilled and the guest attached. **The floor badge** (`tables.html`): a table whose floor read carries `reserved` shows time, name and party size in the info triple, never red, in the flow of the tile rather than floating in a corner. On a phone a 42px two-top is not a name's worth of room, so at 820px the tile keeps time and party (and gives up its seat count, but only on a table nobody is sitting at) and a **Reserved soon** strip under the room carries the whole promise, tappable into the same confirm. Tapping a badged table warns and allows in the spec's own sentence, "Table 9 is held for Bianchi at 12:30 PM, 11 hours ago. Seat anyway?", and confirming runs the covers sheet exactly as before; the booking is not touched, because the host may be about to move it. No engine, route-behavior or store edits; the page formats and never derives. Demo note (headless Chrome over CDP against the live server, real pointer events, both themes at 1280px and a true 390px): three bookings by curl land in the book as "Today · Wed, Sep 2 · 3 bookings · 12 covers" with Lunch 1/6 and Dinner 2/6, two of them past due in amber and Somchai P. carrying IN 20 MINUTES and GUESTBOOK MATCH; the floor badges Table 9 and Table 12 and nothing else; tapping Table 9 gives the warn line verbatim and "Seat anyway" hands straight to the covers sheet titled Table 9; at 390px a booking typed into the sheet (name, phone, party stepped to 4, 11:55 PM) lands the tally on "4 bookings · 16 covers"; Somchai's Seat sheet reads "4 covers · 11:54 PM · in 20 minutes", offers the match OFF and takes it on one tap, and Seat and open check lands on /pos with check #2041 on Table 12 reading "4 covers" and "Somchai P." attached, the book row flipped to Seated and Table 12's badge gone while Table 9 keeps its own. Measured at 390px: page scrollWidth 390 on both screens, every row action 44px, all eight tabs 46px wide with no text overflow. One fix carried on the way: the E23-T2 badge test read a reservation id back out of the day book, so a booking made 30 minutes before midnight landed on tomorrow's service date and the test failed on the clock rather than on the code; it now takes the id off the command's own reply. Suite 149 (141 api.test.ts incl. 2 new page-serve tests + 8 pg.test.ts), typecheck clean, both page scripts parse. Contract: docs/tickets/E23-T3-reservations-ui.md | **Merged** 2026-09-03 · review (Fable) PASSED: independent suite 150/150, typecheck clean, no em dashes; own visual pass at 1280px and 390px confirms the book (period grouping, covers tallies, countdown chips, Seat/No-show/Cancel per row), the Table 12 badge in the info triple with the Reserved soon strip and legend entry, and the compressed phone tile. The 3-line server.ts change is only the page-serve route, ratified. The 42px-tile judgment call (time + party on the tile, the full promise in the strip) accepted as the right phone answer. The midnight flake fix (id off the command reply) is correct and minimal |
| **E21-T3** timezone picker polish | `public/settings.html` only, no engine, route or other-page edits. The picker was already first-party (the browser's own IANA list, filtered from the first keystroke) and shipped no zone data of its own; what it did not do was render anything until somebody typed, which is why the field read as a textbox demanding a precisely spelled zone. **It shows itself now**: focusing an untyped field opens the list immediately with this device's own zone first (`Intl.DateTimeFormat().resolvedOptions().timeZone`, labelled THIS DEVICE), then the zone the venue is currently set to when that differs (labelled SAVED), then the six a US restaurant actually picks from (New York, Chicago, Denver, Los Angeles, Phoenix, Honolulu), deduplicated, with any name this runtime does not know dropped rather than offered as a trap. A footnote says the list is a starting point ("Or type a city to search every zone this browser knows"), and typing filters all 418 exactly as before. The placeholder now says what the field does: "Type a city, or tap a suggestion". **One interpretation worth flagging for review:** the ticket says suggestions appear "on focus with an empty query", but the field is prefilled with the saved zone and is therefore almost never empty, so taken literally the feature would never fire. Untyped is read as "empty, or still holding the zone it already had", which is what makes the discoverability D32 asked for actually happen; clearing the field and typing behaves exactly as written. **Free text cannot be saved**: Save venue checks the value against the browser's own list first and shows the page's existing inline error, "Pick a timezone from the list", instead of posting, then reopens the picker and puts the cursor back in the field. Client-side only, the server contract is unchanged, and the guard stands down when the runtime cannot enumerate zones (the old empty-list fallback), because a page with no list has nothing to judge against and the server is the only judge there is. Demo note (headless Chrome over CDP, real pointer events, Day at 1280px and Night at a true 390px): the list is closed on load; tapping the field opens six 44px suggestions with America/New_York marked THIS DEVICE; typing "auckl" narrows to Pacific/Auckland; "America/Atlantis" opens the manager gate, takes Marco's PIN, and is refused inline with "Pick a timezone from the list" while `GET /v1/venue` still reads America/New_York, so nothing was posted; tapping America/Los_Angeles fills the field and closes the list, and Save venue lands it on the server. On the second pass the list correctly shows America/New_York as THIS DEVICE and America/Los_Angeles as SAVED, proving the dedupe and the label both. 390px measured at scrollWidth 390 with every row and the field at 44px. Suite 150 (142 api.test.ts incl. one new page-serve test + 8 pg.test.ts), typecheck clean, script parses. Contract: docs/tickets/E21-T3-timezone-picker-polish.md | **Merged** 2026-09-03 · review (Fable) PASSED: own focus screenshot shows the list opening untyped with America/New_York marked THIS DEVICE, then the common zones, all 44px; the untyped-means-prefilled-too interpretation ratified (read literally the feature would never fire); the free-text save guard standing down when the runtime cannot enumerate zones is the right fallback; second midnight flake fix (query the booking's own service date) correct |
| **E24-T4** scheduling core | Rung 2 of D28 (D31 lifted the Matt gate), per team-labor-spec §3. **The aggregate**: `PlannedShift` in both stores (migration 0011, expand-only: one table, two indexes, a `CHECK (ends_at > starts_at)` the application cannot talk its way past, and `created_by` genuinely NOT NULL because every write travels a manager's PIN and there is always somebody to name). `roleForShift` is the third thing the spec argued for, neither the E24-T2 job title nor the permission role: it is what somebody is working AS that night, free text, and nothing in the engine branches on it. **The commands** are manager-gated and envelope-idempotent. `upsertPlannedShift` refuses the typos by name (a shift that ends before it starts, "17 hours is a typo rather than a double; a planned shift tops out at 16", an employee the roster does not have, a time that does not parse, and somebody who no longer works here) and WARNS about an overlap without ever refusing it: "Gia R. is already on 11:00 to 15:00 on 2026-09-12. Saved anyway: a split double is real life." A blank `roleForShift` falls back to the employee's own job title rather than to nothing, which keeps a manager from typing "Server" onto every row of a week. `removePlannedShift` is a real delete (nothing points at a shift nobody worked, and the clock's records of what DID happen are never touched). `publishSchedule` flips a whole week in one act, the menu's own draft-then-publish discipline, is idempotent by design ("The week of 2026-09-07 was already published; nothing changed"), and refuses a week with nothing on it rather than pretending to publish air. **A week is named by any day inside it** and always resolves to its Monday, so a manager cannot publish a different seven days than the ones on their screen; the Monday start is a constant rather than a venue setting on the same argument the lunch/dinner split was, and deck F retrofits it. **The reads** are computed and stored nowhere (D19): the manager week (every active employee, seven days, drafts and published together, with an empty row for anybody who has nothing yet so the week is a grid rather than a list), `/v1/schedule/mine` (the signed-in employee's OWN published shifts, on the session alone: a server checking their Tuesday between tables must not go and find a manager, and a draft is invisible there in every circumstance), and planned-versus-actual per employee for one service date, joining published plans against the clock's real `Shift` rows. **HOURS ONLY, and the absence is the feature**: no wage, rate, tax or bank field on the new table or any table it reads, the PG no-wage assertion extended to `planned_shift` and `shift`, and a suite assertion that scans the whole labor payload for money words and finds none. Somebody still on the clock contributes no actual hours and is NAMED (`stillClockedIn`), exactly as the payroll export excludes and names one, because a quietly short number is the failure this rung exists to avoid. **Two judgement calls, both flagged for review.** (1) **The two manager-gated reads are POSTs, not the GETs the ticket specified.** The ticket wrote `GET /v1/schedule?...&managerPin=...`, but a manager's PIN in a query string lands in the server log, the browser history, and any referrer header; E24-T2 met the same question one ticket earlier and answered it in a comment on `/v1/staff/directory`, and E24-T3's hours export followed. `POST /v1/schedule/week` and `POST /v1/insights/labor` follow that precedent rather than re-open a decision this epic already made; the fields and the shapes are otherwise exactly what the ticket asked for, and `/v1/schedule/mine` stays a GET because a deviceId is not a secret. (2) **An edit to an already-published shift stays published.** Reverting it to draft would make the employee's Tuesday vanish from their own screen until somebody remembered to publish again, and a shift that silently disappears is a worse failure than one that visibly moved; the private-until-published property still holds where it matters, because a new shift is always a draft and next week is all drafts until the week is published once. Demo note (live curl against the running server, memory store): a server's PIN is refused at every manager door; Marco plans Gia a Tuesday bar shift and himself Saturday expo; a third shift overlapping Gia's warns and saves; 22:00-to-16:00 and a 17-hour shift are each refused by name; Gia signs in and her own week reads zero shifts while the manager's week reads three drafts; one publish turns 3 drafts into 3 published and Gia's Tuesday appears with `from 16:00 to 22:00, hours 6`; publishing again reports 0 published and 3 already published; and the labor line for a day reads planned 6 hours against actual 0 for a server still on the clock, with `stillClockedIn: true` beside it and the posture sentence in the payload. The arithmetic with real differences is proven on a seeded day in the suite (planned 6 / worked 7 / +1, an unplanned worker at +7, and a still-clocked-in row at -6). 160 tests (151 api.test.ts incl. 9 new + 9 pg.test.ts incl. 1 new), typecheck clean, PG round trip through two restarts proving the draft gate survives one and the delete is real. Contract: docs/tickets/E24-T4-scheduling-core.md | **Implemented** 2026-09-03 · awaiting review |
| **E24-T5** scheduling UI | The /schedule page (9th rail entry, phone-first): an employee's own week as the default view (five-second read, no PIN), the manager grid behind the PIN (one day at a time on a phone, never a sideways scroll), Publish week as one deliberate confirmed act, planned-vs-actual hours line. Contract: docs/tickets/E24-T5-scheduling-ui.md | Ready after E24-T4 · Sonnet/Codex (D22 batch with E26-T1) |
| **E25-T1** roles core | D33: the permission enum grows to owner\|manager\|kitchen\|server (migration 0012, expand-only; seed gains an owner and a chef), ONE visibility matrix served with the session, enforced server-side on reads and mutations (nav hiding is courtesy, the route refusal is the rule); owner outranks manager (venue identity, pay period, and role changes on managers are owner-gated), kitchen and server are lateral; last-manager guard becomes last-owner-or-manager. Titles stay vocabulary per D28. Contract: docs/tickets/E25-T1-roles-core.md | Ready after E24-T4 · Opus |
| **E25-T2** roles UI | Every page's rail renders only the signed-in role's screens (absent, not grayed); a direct URL outside the role gets a calm named refusal page; Settings' role picker gains Owner and Kitchen with one-line explanations; the lock screen shows the role under the name. Matrix comes from the session response, hardcoded nowhere. Contract: docs/tickets/E25-T2-roles-ui.md | Ready after E25-T1 · Sonnet/Codex |
| **E26-T1** inventory spec | Spec only (D34): ingredient-level stock with per-serving recipe links (Branzino al Forno depletes one fish; modifier options can deplete their own ingredient), derived on-hand (count baseline + receipts - fired quantities; a physical count resets, because kitchens spill), par levels, the chef's low-stock and order-list views, the 86-proposal bridge (never auto-86), bar as the same mechanism by station. Founder-question table with shippable defaults; ANDY is the domain reviewer, not Matt. Builds gate on his review. Contract: docs/tickets/E26-T1-inventory-spec.md | Ready · Sonnet/Codex (D22 batch with E24-T5) |
| Next after these | Inventory build tickets after the founder reviews E26-T1's spec; vendor converters when a real export file exists; guest imports on Matt's consent deck; E13 needs ADR-3; E9/E10 need D6; online booking stays integrate-never-build | Queued |

Suite total: 220 tests green (73 domain + 147 server incl. PostgreSQL integration).

## Flagship mockup: done since first cut

Tracked here because the mockup is the Phase 0 conversation tool, so its state matters to WP-0.2.

| Date  | Change                                                                                                                |
| ----- | --------------------------------------------------------------------------------------------------------------------- |
| 08-11 | Check switching, working table transfer, cancel-empty-check, More menu with stubs                                     |
| 08-11 | Reversible KDS (back-step, recall window) instead of one-way bump                                                     |
| 08-11 | Spatial floor plan: positioned tables, shapes, decor, legend                                                          |
| 08-11 | Phone layout: bottom tabs, check bottom sheet, horizontal category rail                                               |
| 08-11 | Em dashes removed repo-wide; style rule recorded (D12)                                                                |
| 08-12 | KDS rebuilt on founder feedback: one card per table,`New` flags, per-item bump, expo-only serve, cook-together pane |
| 08-12 | Editable party size with seat-assignment guard; seat dots on floor plan                                               |
| 08-12 | Theme transitions, late pulse, focus-visible, reduced-motion (ported from a Gemini pass, minus its CDN font links)    |
| 08-12 | Phone viewport height fix (`--vph`), stock badge no longer overlaps item names                                      |

## Non-blocking side items

- Publish the mockup and master plan as private artifacts to share with Matt (optional, on request)
- Decide whether to keep or delete `prototypes/index_RestaurantOS_Gemini.html` now that its changes are merged
- Mockup-raised operator questions are consolidated in master plan section 9 and the session guide (deck A2)
