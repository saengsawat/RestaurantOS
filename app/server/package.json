{
  "name": "@restaurantos/server",
  "version": "0.1.0",
  "private": true,
  "description": "RestaurantOS API server: the command protocol over the real domain engine.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@restaurantos/domain": "file:../domain",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
